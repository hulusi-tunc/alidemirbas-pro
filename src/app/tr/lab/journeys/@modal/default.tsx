/* Nothing to show in the modal slot unless a journey route is intercepted -
   including after a refresh, where the full page renders in `children`. */
export default function Default() {
  return null;
}
