# CONTEXT.md — <project name>

Verified project truth. Written during workflow.md's BRIEF and REGISTER
stages, in DISCOVER mode (modes.md). Every field states a fact or names an
open question — never a guess presented as a fact. Amended, not rewritten,
as understanding improves; changes append a dated line rather than silently
replacing the old one (core P14).

## What we're making

<one or two sentences: the thing itself, not the visual treatment>

## Why it exists / what job it does for a visitor

<the user job — see registers.md's classification procedure question 1>

## Audience

<who arrives, from where, wanting what>

## Register

<declared register + sub-mode + one-line reason, per registers.md's
classification procedure — this is the single most consequential fact in
this file; get it named explicitly, not implied>

## Constraints

- Non-negotiable requirements from the brief (top of core P16's ladder)
- Technical constraints (existing stack, hosting, timeline)
- Anything explicitly out of scope

## Success

<what "working" looks like — for the project, not just for one page>

## Open questions

<unresolved unknowns, named rather than silently assumed — workflow.md
stage 1's own failure mode is "proceeding on an unstated assumption without
declaring it">

## Decision log

<dated one-liners, append-only, for anything in this file that changed
after the first write and why>
