# DESIGN.md — <project name>

Preserves **decisions**, not just tokens. A token list tells a future
session what the values are; it doesn't tell it why, which means every
amendment risks re-deriving (or contradicting) a choice that was already
made deliberately. This file is the record that makes that unnecessary.

This is the project's **site-constitution.md instance** (Layer 1 + Layer 2
content, per that module) plus the standing record of the decisions that
produced it. It is not a duplicate of site-constitution.md's generic
mechanism — that file stays generic; this file is what results from running
it on this specific project.

## Register

<the declared register(s) — site shell vs. page-family, if they differ, per
registers.md's hybrid rule>

## Visual material strategy

<the one-line declared strategy from visual-material-strategy.md, and why>

## Selected direction

<which manifesto won the fan-out (visual-direction.md), one line, plus the
manifesto itself if it's short enough to inline — otherwise a pointer to
where it's archived in project memory>

## Site constitution — Layer 1 (global)

<typography, color, spacing, container widths, navigation, footer, controls,
border/radius language, imagery honesty rules, motion vocabulary, copy
register — the actual values this project committed to, per
site-constitution.md's Layer 1 table>

## Page-family rules — Layer 2

<per family: composition skeleton, anchor type norm, density band, grammar
subset, background transition pattern, page-to-page transition behavior>

## Technical fit

<the one-line stack decision from technical-fit.md>

## Recorded exceptions

<site-constitution.md's exception format, inlined or pointered — every
approved deviation from the layers above, with its content reason and
render evidence>

## Decision log

<dated one-liners: what changed in this file, and why — append-only, per
core P14>
