# Semantic Event Registry — build report

Generated by `scripts/build-event-registry.mjs` from `production/canonical-dump.json`, `production/surface-assignment.json` and `scripts/event-curation.json`. Registry written to `src/canonical/events.ts`.

| Measure | Count |
|---|--:|
| Raw trigger events (one per journey) | 283 |
| Distinct trigger event ids registered | 282 |
| Trigger ids shared by more than one journey (legitimate: one event, several journeys) | 1 |
| Distinct wait phrases on the customer surface | 170 |
| …of which registered as their own event | 151 |
| …of which merged into an existing id as a declared equivalent | 6 |
| …unresolved (must be 0 for the customer surface) | 0 |
| Distinct wait phrases on operational/mechanism surfaces (retained as prose until Gate 5) | 156 |
| Measurement / new-journey events from curation | 22 |
| **Canonical events in the registry** | **455** |
| Ambiguous entries kept as one with a review note | 0 |
| Id collisions between origins | 3 |
| Identical meaning text under two ids (must be 0) | 0 |

## Trigger ids shared by several journeys
- first_party_interest_captured (ACQ-02, ACQ-285)

## Aliases merged (a person declared these phrases equivalent)
- trigger process_started takes its curated meaning
- trigger selection_recorded takes its curated meaning
- trigger expected_depletion_approaching takes its curated meaning
- trigger lapsed_customer_detected takes its curated meaning
- trigger interest_signal_recorded takes its curated meaning
- "the request is withdrawn" → request_withdrawn

## Ambiguous, kept separate or flagged for review
- none

## Collisions
- cancellation_confirmed: curated wait event reuses a trigger event id - treated as the same event (a journey waits on what another journey starts on); the trigger's evidence stays its meaning
- frequency_preference_changed: curated wait event reuses a trigger event id - treated as the same event (a journey waits on what another journey starts on); the trigger's evidence stays its meaning
- relationship_ended: curated wait event reuses a trigger event id - treated as the same event (a journey waits on what another journey starts on); the trigger's evidence stays its meaning

## Identical meanings under different ids
- none

## Unresolved — customer surface (blocking)
- none

## Unresolved — operational and mechanism surfaces (retained as prose; Gate 5)
152 phrases, e.g.:
- the required propagation confirmations arrive
- the contact point becomes reachable again
- an authoritative change to the condition that created the cooldown
- authoritative reconciliation completes
- a manual resolution is recorded
- the owner accepts
- the owner rejects
- the owner acknowledges the critical items
- the incoming owner accepts
- the incoming owner declines
- a decision is provided
- the blocker is resolved
- approved
- rejected
- changes requested
- the required decision set resolves
- a blocking rejection arrives
- changes are requested by any approver
- the action or subject version changes underneath the package
- the required re-approvals resolve
- the subject is revised
- the condition that justified the exception is resolved
- a permanent decision is made about it
- the exit condition is satisfied
- a reversal is requested
- … and 127 more

## Source-class distribution of the registry
- authoritative: 346
- declared: 75
- behavioral: 26
- inferred: 8
