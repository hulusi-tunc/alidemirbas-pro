# vNext migration changelog

Generated by `scripts/vnext-changelog.mjs`. One entry per Customer Journey (135); semantic changes only. "Reviewer" lines quote production/vnext-migration-reviews.json.

## Merged ids

- CON-37 → CMS-208 (delivery-recovery): 'CON-37 was consolidated into CMS-208' — the mechanism this id used to own now lives on Delivery failure → classify → retry, fallback or stop
- CMS-209 → CON-36 (channel-contactability-state): 'CMS-209 was consolidated into CON-36' — the mechanism this id used to own now lives on Channel contactability change → recalculate reachability → route or suppress
- CTL-239 → OWN-57 (multi-party-approval-aggregation): 'CTL-239 was consolidated into OWN-57' — the mechanism this id used to own now lives on Multi-party approval → aggregate required decisions → authorised or blocked
- CTL-240 → OWN-54 (ownership-change-obligation-transfer): 'CTL-240 was consolidated into OWN-54' — the mechanism this id used to own now lives on Ownership change → transfer obligations → continue
- RET-25 → RSK-192 (risk-signal): 'RET-25 was consolidated into RSK-192' — the mechanism this id used to own now lives on Risk signal → correlate evidence → clear, monitor, restrict or review
- FBK-44 → FBK-43 (feedback-routing-and-loop-closure): 'FBK-44 was consolidated into FBK-43' - Negative feedback recovery was folded into feedback follow-up: one feedback record, one router.
- FBK-45 → FBK-43 (feedback-routing-and-loop-closure): 'FBK-45 was consolidated into FBK-43' - Positive feedback follow-up was folded into feedback follow-up: one feedback record, one router.
- ACT-15 → ACT-17 (early-adoption-to-stable-use): 'ACT-15 was consolidated into ACT-17' - First-value recognition became the opening step of adoption nurture rather than a lifecycle of its own.

## Journeys

### ACC-261 · Access Recovery — migrated (Gate 4; communicating)
- entity instance: `account_id + restriction_id` · one-active-per-key
- eligibility 4 · suppressions 4 (CCCC)
- waits → Config: w.resolve: access_restriction.resolve (required) [attribute-bound, attribute@resolution_deadline_at]
- exits classed: x.informed=success, x.security-owned=suppression, x.restored=success, x.stands=timeout
- orchestration: offer-decide-remind, 3 touch(es) [t1:persistent>in-session, t2:persistent>in-session, t3←w.resolve after t2:persistent>in-session], no-action 4; contact service/service, cap access_restriction.touches = 2 [corpus-rule] (all), cooldown access_restriction.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (5 refs); business restriction_lifted self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the account and the restriction. The inform-only and actionable notices are the exclusive arms of c.actionable; the actionable notice carries the resolving condition, the confirmation carries none by design. The wait is bound to the restriction's own deadline. Exit classes corrected. At most two touches. Outcome: the restriction lifted, compared pre/post. Draft purpose text repaired. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.measurement.businessOutcome, journey.implementation.attributes.required, touch t1, touch t2, wait w.resolve, exits, aliases, useCases)
- warnings reviewed: touch_no_destination (t3): The restoration confirmation asks for nothing; it names what returned and carries no route by design.

### ACC-263 · Activation Reminder — migrated (Gate 4; communicating)
- entity instance: `entitlement_id + holder_id` · one-active-per-key
- eligibility 5 · suppressions 4 (CCCC)
- waits → Config: w.first-use: entitlement_activation.first_use (required) [reminder-before-attribute, attribute@activation_window_ends_at]; w.last-chance: entitlement_activation.last_chance (required) [attribute-bound, attribute@activation_window_ends_at]
- exits classed: x.activated=success, x.moot=invalid-state, x.lapsed=timeout
- orchestration: offer-decide-remind, 3 touch(es) [t1:persistent>in-session, t2:persistent>in-session, t3←w.first-use:persistent>in-session], no-action 4; contact service/service, cap entitlement_activation.touches = 2 [corpus-rule] (all), cooldown entitlement_activation.cooldown = none [corpus-rule], competition none
- measurement: journey exit (3 refs); business capability_first_used self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the entitlement and its holder. The first-time and familiar notices are the exclusive arms of c.first-time; the brief confirmation carries no route by design, the first-time notice and the reminder carry the first action. Both waits are bound to the activation window. At most two touches. Outcome: first use, compared pre/post. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t1, touch t3, wait w.first-use, wait w.last-chance, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): The first-time notice and the brief confirmation are the exclusive arms of c.first-time. Never both. | touch_no_destination (t2): The brief confirmation to a familiar holder names only what changed and carries no route by design.

### ACC-71 · Entitlement Qualification — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + entitlement_key` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- exits classed: x.reconciled=success, x.pending=no-action, x.denied=failure
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the account and the entitlement. A pending requirement is a no-action state; a reconciled existing right is a correct outcome. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### ACC-78 · Access Suspension — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + capability_scope` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.suspension: access_suspension.suspension (required) [attribute-bound, attribute@review_point_at]
- exits classed: x.extended=success
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the account and the capability scope. The wait is bound to the suspension's review point. (changed: entity.instanceKey, journey.implementation.attributes.required, wait w.suspension, aliases, useCases)

### ACC-79 · Capability Restoration — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + restoration_case_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.restored=success, x.partial=success, x.remains=no-action
- measurement: journey exit (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the account and the restoration case. Nothing qualifying for restoration is a no-action state. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### ACQ-01 · Anonymous Identity Resolution — migrated (Gate 4; silent lifecycle state)
- entity instance: `anonymous_profile_id` · one-active-per-key
- eligibility 6 · suppressions 3 (CCC)
- waits → Config: w.identity: anonymous_intent.identity (required) [observation-window, trigger]
- exits classed: x.stale=timeout, x.known-only=success
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the anonymous profile, not on ids it does not yet have. Stale intent is a timeout, not an invalid state. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### ACQ-02 · Interest Qualification Routing — migrated (Gate 4; silent lifecycle state)
- entity instance: `lead_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: 
- measurement: journey handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Pure router by design: every capture ends in a handoff. Keyed on the lead. (changed: entity.instanceKey, journey.implementation.attributes.required, aliases, useCases)
- warnings reviewed: router_journey: Interest routing ends in a handoff on every path by design: a named destination, a disqualifier, or education.

### ACQ-03 · Intent Escalation Handoff — migrated (Gate 4; silent lifecycle state)
- entity instance: `person_id + intent_entity_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: x.unchanged=no-action, x.retained=success
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the person and the entity the intent is about. Noise is a no-action state. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### ACQ-04 · High-Intent Lead Routing — migrated (Gate 4; silent lifecycle state)
- entity instance: `lead_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: 
- orchestration: single-notice, 1 touch(es) [t1:human], no-action 4; contact service/none, cap high_intent.touches = 1 [corpus-rule] (all), cooldown high_intent.cooldown = none [corpus-rule], competition purchase-intent
- measurement: journey handoff (3 refs); no business outcome
- discovery: 5 aliases
- reviewer: Human routing, not customer communication: both channels are human, pressure class none. One decision per lead action, no cooldown. Pure router. Keyed on the lead. (changed: entity.instanceKey, journey.contact.defaultPriority, journey.contact.pressureClass, journey.contact.cooldown, journey.channelStrategy.roles, journey.implementation.attributes.required, aliases, useCases)
- warnings reviewed: router_journey: High-intent routing decides who owns the next step and hands to them: an assigned person, automated continuation, or the already-reached destination.

### ACQ-05 · Qualification State Routing — migrated (Gate 4; silent lifecycle state)
- entity instance: `lead_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.recycle: qualification_state.recycle (required) [attribute-bound, attribute@recycle_horizon_at]; w.requirement: qualification_state.requirement (required) [attribute-bound, attribute@requirement_validity_ends_at]
- exits classed: x.in-progress=no-action, x.not-yet=no-action, x.terminal=invalid-state, x.recycled=success, x.recycle-expired=timeout, x.requirement-lapsed=timeout
- measurement: journey exit-or-handoff (8 refs); no business outcome
- discovery: 5 aliases
- reviewer: Keyed on the lead. Recycle and requirement waits are bound to the horizon recorded with the reason, not to the trigger. Exit classes corrected: qualifying and unqualified are no-action states, a terminal mismatch is an invalid state, a lapsed requirement is a timeout. (changed: entity.instanceKey, journey.implementation.attributes.required, wait w.recycle, wait w.requirement, exits, aliases, useCases)

### ACQ-06 · Eligibility Recalculation — migrated (Gate 4; silent lifecycle state)
- entity instance: `entity_ref + rule_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.eligible=success, x.ineligible=success
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies the trigger must not start on. Keyed on the entity the rule is about and the rule. Both outcomes are correct decisions. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, trigger, aliases, useCases)

### ACQ-07 · Intent Decay — migrated (Gate 4; silent lifecycle state)
- entity instance: `person_id + intent_context_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.cooldown: intent_decay.cooldown (required) [cooldown, trigger]
- exits classed: x.unchanged=no-action, x.cooled=timeout
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the person and the intent context. The wait is a cooldown by nature; maintained intent is a no-action state. (changed: entity.instanceKey, journey.implementation.attributes.required, wait w.cooldown, exits, trigger, aliases, useCases)

### ACQ-08 · Acquisition Exit Handoff — migrated (Gate 4; silent lifecycle state)
- entity instance: `person_id + destination_entity_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: x.not-conversion=invalid-state
- measurement: journey exit-or-handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the person and the destination entity. A proxy that is not a conversion is an invalid state, not a suppression. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### ACQ-09 · Lead Nurture — migrated (Gate 4; communicating)
- entity instance: `lead_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.window: bounded_education.window (required) [observation-window, trigger]
- exits classed: x.no-basis=no-action, x.permission-ended=suppression, x.unreachable=suppression, x.sunset=timeout
- orchestration: single-notice, 1 touch(es) [t1:persistent>in-session], no-action 4; contact promotional/promotional, cap bounded_education.touches (required) (all), cooldown bounded_education.cooldown (required), competition none
- measurement: journey exit-or-handoff (5 refs); business nurture_progression_signal self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: The graph models the education as one action standing for a bounded sequence, so the local cap is the sequence length the company fixes at entry - a required config, not the plan's action count. The window runs from entry, not from the first touch. No-basis is a no-action state; permission and route loss are suppressions. Progression compared pre/post. (changed: entity.instanceKey, journey.contact.localCap, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, wait w.window, exits, aliases, useCases)

### ACQ-10 · Commercial Decline Routing — migrated (Gate 4; silent lifecycle state)
- entity instance: `lead_id + decline_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.reentry: commercial_decline.reentry (required) [attribute-bound, attribute@reentry_at]
- exits classed: x.terminal=invalid-state, x.cooldown=no-action
- measurement: journey exit-or-handoff (5 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the lead and the decline. The re-entry wait is bound to the recorded date. A terminal mismatch is an invalid state. (changed: entity.instanceKey, journey.implementation.attributes.required, wait w.reentry, exits, aliases, useCases)

### ACQ-11 · Abandoned Process Recovery — NEW canonical journey (Gate 2; communicating)
- entity instance: `person_id + logical_process_id` · one-active-per-key · supersession stated
- eligibility 5 · suppressions 8 (CCCCCRRO)
- waits → Config: w.abandon: recovery.first_check = 30 minutes–60 minutes [example-only] [recovery-window, attribute@last_activity_at]; w.second: recovery.second_check = 20 hours–28 hours [example-only] [recovery-window, previous-touch]; w.resumed: recovery.first_check = 30 minutes–60 minutes [example-only] [recovery-window, attribute@last_activity_at]; w.final: recovery.lifetime = 3 days–7 days [example-only] [recovery-window, trigger]; w.close: recovery.process_expiry = expires_at as asserted by the platform [attribute-bound] [attribute-bound, attribute@expires_at]
- exits classed: x.converted=success, x.invalid=invalid-state, x.superseded=suppression, x.no-action=no-action, x.lapsed=timeout
- loop budgets: a.note-return: recovery.resume_rearms = 1 [corpus-rule]
- orchestration: progressive-recovery, 3 touch(es) [t1←w.abandon:low-friction>persistent, t2←w.second after t1:persistent>low-friction, t3←w.final after t2:persistent>urgent], no-action 7; contact promotional/promotional, cap recovery.touches = 3 [corpus-rule] (all), cooldown recovery.cooldown = 7 days–30 days [example-only], competition commerce-recovery
- measurement: journey exit-or-handoff (6 refs); business process_completed self · touched-before-event · persistent-holdout
- discovery: 7 aliases, presets checkout-abandonment, quote-abandonment, application-abandonment, incomplete-registration

### ACQ-12 · Abandoned Selection Recovery — NEW canonical journey (Gate 3; communicating)
- entity instance: `person_id + selection_id` · one-active-per-key · supersession stated
- eligibility 5 · suppressions 8 (CCCCCCRO)
- waits → Config: w.settle: selection.first_check = 1 hour–4 hours [example-only] [recovery-window, attribute@last_selection_activity_at]; w.second: selection.second_check = 2 days–4 days [example-only] [recovery-window, previous-touch]; w.final: selection.lifetime = 7 days–14 days [example-only] [recovery-window, trigger]
- exits classed: x.converted=success, x.invalid=invalid-state, x.unavailable=invalid-state, x.no-action=no-action, x.lapsed=timeout
- loop budgets: a.rearm: selection.change_rearms = 2 [corpus-rule]
- orchestration: progressive-recovery, 2 touch(es) [t1←w.settle:low-friction>persistent, t2←w.second after t1:persistent>low-friction], no-action 7; contact promotional/promotional, cap selection.touches = 2 [corpus-rule] (all), cooldown selection.cooldown = 14 days–30 days [example-only], competition commerce-recovery
- measurement: journey exit-or-handoff (6 refs); business selection_converted self · touched-before-event · persistent-holdout
- discovery: 7 aliases, presets cart-abandonment, saved-item-reminder

### ACQ-13 · Unresolved Interest Recovery — NEW canonical journey (Gate 3; communicating)
- entity instance: `person_id + interest_key` · one-active-per-key · supersession stated
- eligibility 6 · suppressions 6 (CCCCCR)
- waits → Config: w.settle: interest.settle_window = 12 hours–48 hours [example-only] [recovery-window, attribute@last_interest_at]; w.after: interest.lifetime = 3 days–7 days [example-only] [observation-window, previous-touch]
- exits classed: x.resolved=success, x.unavailable=invalid-state, x.no-action=no-action, x.lapsed=timeout
- orchestration: single-notice, 1 touch(es) [t1←w.settle:low-friction>persistent], no-action 6; contact promotional/promotional, cap interest.touches = 1 [corpus-rule] (all), cooldown interest.cooldown = 14 days–30 days [example-only], competition commerce-recovery
- measurement: journey exit (4 refs); business purchase_completed self · touched-before-event · persistent-holdout
- discovery: 6 aliases, presets browse-abandonment, product-view-abandonment, search-abandonment

### ACQ-285 · New Lead Welcome — migrated (Gate 4; communicating)
- entity instance: `capture_id` · one-active-per-key
- eligibility 5 · suppressions 4 (CCCC)
- waits → Config: w.nurture: captured_interest.nurture (required) [observation-window, previous-touch]
- exits classed: x.no-delivery-route=failure, x.stopped=suppression, x.sunset=timeout, x.delivered=success
- orchestration: notice-then-confirm, 3 touch(es) [t1:persistent, t2:persistent, t3 after t2:persistent], no-action 4; contact promotional/promotional, cap captured_interest.touches = 3 [corpus-rule] (all), cooldown captured_interest.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (5 refs); business destination_declared self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the individual capture. The first touch and the delivery are the exclusive arms of what the person declared. The sequence opener carries the declared subject as destination. Per capture, so no cooldown. Exit classes corrected. Outcome: a destination declared, compared pre/post. (changed: entity.instanceKey, journey.contact.cooldown, journey.measurement.businessOutcome, journey.implementation.attributes.required, touch t3, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): t1 (a person will follow up) and t2 (deliver the material) are the exclusive arms of c.declared - what the person asked for. Never both.

### ACT-11 · Onboarding Route Assignment — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + onboarding_instance_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: 
- orchestration: single-notice, 1 touch(es) [t1:human], no-action 3; contact lifecycle/lifecycle, cap onboarding_route.touches = 1 [corpus-rule] (all), cooldown onboarding_route.cooldown = none [corpus-rule], competition none
- measurement: journey handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Human-only route assignment. Draft keyed the instance on subscription+account from the entity prose; the thing entered is the onboarding instance. Cooldown is per instance (none). Pure router: both outcomes are handoffs by design. (changed: entity.instanceKey, journey.contact.cooldown, journey.implementation.attributes.required, aliases, useCases)
- warnings reviewed: router_journey: Route assignment chooses a path and hands over: blocked start to ACT-13, clear start to ACT-12. It owns no outcome of its own.

### ACT-12 · Onboarding Nurture — migrated (Gate 3; communicating)
- entity instance: `account_id + onboarding_instance_id` · one-active-per-key
- eligibility 4 · suppressions 6 (CCCCCC)
- waits → Config: w.progress: onboarding.step_interval (required) [observation-window, previous-touch]
- exits classed: x.window-closed=timeout
- loop budgets: a.surface: onboarding.step_prompts = 5 [example-only]
- orchestration: progressive-recovery, 1 touch(es) [t1:in-session>persistent], no-action 6; contact lifecycle/lifecycle, cap onboarding.step_prompts = 5 [example-only] (all), cooldown onboarding.cooldown = 14 days–30 days [example-only], competition lifecycle-stage
- measurement: journey exit-or-handoff (3 refs); business activation_recorded self · touched-before-event · persistent-holdout
- discovery: 5 aliases

### ACT-13 · Onboarding Blocker Reminder — migrated (Gate 4; communicating)
- entity instance: `account_id + requirement_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.resolve: activation_blocker.resolve (required) [observation-window, trigger]
- exits classed: x.not-blocking=invalid-state, x.next-blocker=success, x.blocked=failure
- orchestration: single-notice, 2 touch(es) [t1:persistent>in-session, t2:human], no-action 3; contact lifecycle/lifecycle, cap activation_blocker.touches = 2 [corpus-rule] (all), cooldown activation_blocker.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (6 refs); business named_requirement_satisfied self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the requirement, not only the account. The two touches are the exclusive arms of c.self-resolvable (self-resolvable vs someone else's). The specific action carries a destination. One touch per requirement, so no cooldown. Business outcome compared pre/post. (changed: entity.instanceKey, journey.orchestration.strategy, journey.contact.cooldown, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t1, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): t1 (specific action to the account) and t2 (raise with the dependency owner) are the exclusive arms of c.self-resolvable. Never both.

### ACT-14 · Onboarding Help — migrated (Gate 4; communicating)
- entity instance: `account_id + person_id` · one-active-per-key
- eligibility 4 · suppressions 4 (CCCC)
- waits → Config: w.response: struggling_user.response (required) [response-window, previous-touch]; w.session: struggling_user.session (required) [observation-window, previous-touch]
- exits classed: x.not-eligible=no-action, x.defer=suppression, x.declined=no-action, x.no-outcome=timeout, x.normal=success
- orchestration: offer-decide-remind, 4 touch(es) [t1:persistent>in-session>low-friction, t2←w.response after t1:persistent>in-session>low-friction, t3←w.session after t2:persistent>in-session>low-friction, t4←w.response after t1:persistent>in-session>low-friction], no-action 4; contact lifecycle/lifecycle, cap struggling_user.touches = 4 [corpus-rule] (all), cooldown struggling_user.cooldown (required), competition none
- measurement: journey exit-or-handoff (6 refs); business activation_recorded self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Touch order fixed (final option follows the offer). Offer and confirmation carry destinations. Exit classes corrected: not-eligible and declined are no-action states, no-outcome is a timeout. Waits run from the previous touch. Outcome measured on activation, not on the booking. (changed: journey.measurement.businessOutcome, journey.implementation.attributes.required, touch t1, touch t2, touch t4, wait w.response, wait w.session, exits, aliases, useCases)

### ACT-16 · Onboarding Completion Handoff — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + person_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: x.not-activation=invalid-state
- measurement: journey exit-or-handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Silent close-out. A proxy that is not activation is an invalid-state exit, not a success. (changed: exits, aliases, useCases)

### ACT-17 · Adoption Nurture — migrated (Gate 3; communicating)
- entity instance: `account_id + use_case_id` · one-active-per-key
- eligibility 4 · suppressions 5 (CCCCC)
- waits → Config: w.observe: adoption.observation_window (required) [observation-window, previous-touch]
- exits classed: 
- loop budgets: a.next-behavior: adoption.nudge_budget = 3 [example-only]
- orchestration: single-notice, 3 touch(es) [t0:in-session>persistent, t1 after t0:in-session>persistent, t2 after t0:in-session>persistent], no-action 5; contact lifecycle/lifecycle, cap adoption.touches = 4 [example-only] (all), cooldown adoption.cooldown = 30 days–90 days [example-only], competition none
- measurement: journey exit-or-handoff (2 refs); business value_produced self · touched-before-event · pre-post
- discovery: 6 aliases
- warnings reviewed: router_journey: Adoption nurture ends only in handoffs by design: stable adoption goes to the lifecycle owner, a stall goes to adoption recovery. There is no outcome it owns alone.

### ACT-18 · Adoption Recovery — migrated (Gate 3; communicating)
- entity instance: `account_id + use_case_id` · one-active-per-key
- eligibility 4 · suppressions 5 (CCCCC)
- waits → Config: w.recover: adoption_recovery.window (required) [recovery-window, previous-touch]
- exits classed: x.satisfied=invalid-state, x.no-intervention=no-action
- orchestration: single-notice, 1 touch(es) [t1:persistent>low-friction], no-action 5; contact lifecycle/lifecycle, cap adoption_recovery.touches = 1 [corpus-rule] (all), cooldown adoption_recovery.cooldown = 30 days–90 days [example-only], competition retention-outreach
- measurement: journey exit-or-handoff (5 refs); business value_produced self · touched-before-event · persistent-holdout
- discovery: 6 aliases

### ACT-19 · Onboarding Personalization — migrated (Gate 4; communicating)
- entity instance: `account_id + person_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.answer: role_use.answer (required) [response-window, previous-touch]
- exits classed: x.dont-ask=no-action
- orchestration: single-notice, 1 touch(es) [t1:persistent>in-session], no-action 3; contact lifecycle/lifecycle, cap role_use.touches = 1 [corpus-rule] (all), cooldown role_use.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (2 refs); business question_answered self · touched-before-event · none
- discovery: 4 aliases
- reviewer: One question per onboarding instance: no cooldown. Not asking is a no-action state. The question carries its form as destination; the wait runs from the ask. (changed: journey.contact.cooldown, journey.measurement.businessOutcome.comparison, touch t1, wait w.answer, exits, aliases, useCases)

### ACT-20 · Dormant Lead Reactivation — migrated (Gate 4; communicating)
- entity instance: `lead_id + context_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.return: dormant_non.return (required) [response-window, previous-touch]
- exits classed: x.winback=invalid-state, x.no-reason=no-action, x.engagement-only=invalid-state, x.sunset=timeout
- orchestration: single-notice, 1 touch(es) [t1:persistent>low-friction], no-action 3; contact promotional/promotional, cap dormant_non.touches = 1 [corpus-rule] (all), cooldown dormant_non.cooldown (required), competition lifecycle-stage
- measurement: journey exit-or-handoff (7 refs); business meaningful_return self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: A never-paid relationship is promotional pressure, not lifecycle. Keyed on the lead and its context. Out-of-scope and no-reason are not successes. Success is a meaningful return by the corpus's own rule - the wait advances on it deliberately. (changed: entity.instanceKey, journey.contact.defaultPriority, journey.contact.pressureClass, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t1, wait w.return, exits, aliases, useCases)
- warnings reviewed: engagement_as_progression (meaningful_return): The corpus's own rule: success is a meaningful return - a real action in the product - never an open or a click. The event is inspected (a.inspect) before anything advances.

### CON-264 · Contact Verification — migrated (Gate 3; communicating)
- entity instance: `contact_point_id` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- waits → Config: w.confirm: contact_verification.confirmation_window (required) [response-window, previous-touch]; w.last: contact_verification.window_remainder = the remainder of the confirmation window, counted from the first request [attribute-bound] [attribute-bound, attribute@confirmation_window_ends_at]
- exits classed: x.permitted=success, x.lapsed=timeout, x.superseded=invalid-state
- orchestration: two-party-confirmation, 3 touch(es) [t-old mandatory:persistent>urgent, t-new mandatory:persistent>urgent, t-remind←w.confirm after t-new:persistent>urgent], no-action 5; contact security/none, cap contact_verification.reminders = 1 [corpus-rule] (non-mandatory), cooldown contact_verification.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business destination_confirmed self · entered-before-event · not-applicable
- discovery: 6 aliases
- warnings reviewed: simultaneous_review (t-old, t-new): t-old and t-new are both sent, in sequence, to two different destinations: the replaced destination is warned, then the new destination is asked to confirm. This is the two-party confirmation itself, not a simultaneous send to one person's channels; the channel is the destination's own kind.

### CON-272 · Contact Recovery — migrated (Gate 4; communicating)
- entity instance: `contact_point_id + person_id` · one-active-per-key
- eligibility 4 · suppressions 5 (CCCCC)
- waits → Config: w.corrected: contactability_repair.corrected (required) [observation-window, previous-touch]
- exits classed: x.dark=suppression, x.repaired=success, x.recovered=success, x.suppressed=suppression
- orchestration: offer-decide-remind, 3 touch(es) [t1 mandatory:in-session, t2 mandatory:urgent>persistent, t3←w.corrected mandatory:in-session>persistent>urgent], no-action 5; contact service/none, cap contactability_repair.discretionary_touches = 0 [corpus-rule] (non-mandatory), cooldown contactability_repair.cooldown = none [corpus-rule], competition none
- measurement: journey exit (4 refs); business replacement_destination_verified self · touched-before-event · not-applicable
- discovery: 5 aliases
- reviewer: A contactability repair is service communication, exempt from caps but not security-class. The two prompts are the exclusive arms of c.route and each goes only on a route that still works. Every touch is mandatory, so the discretionary cap is zero. The confirmation carries no call to action by design. (changed: journey.contact.defaultPriority, journey.contact.localCap, journey.implementation.attributes.required, touch t1, touch t2, touch t3, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): t1 (in product) and t2 (surviving off-product route) are the exclusive arms of c.route - what can carry the repair request. Never both. | touch_no_destination (t3): The confirmation states that the replacement is in use; it asks for nothing and carries no route by design.

### CON-283 · Frequency Preference Update — migrated (Gate 4; communicating)
- entity instance: `person_id` · one-active-per-key
- eligibility 5 · suppressions 4 (CCCC)
- waits → Config: w.cycle: frequency_reduction.cycle (required) [observation-window, previous-touch]
- exits classed: x.opted-out=no-action, x.reduced-again=success, x.holding=success
- orchestration: single-notice, 1 touch(es) [t1 mandatory:persistent], no-action 4; contact service/none, cap frequency_reduction.touches = 1 [corpus-rule] (non-mandatory), cooldown frequency_reduction.cooldown = none [corpus-rule], competition none
- measurement: journey exit (3 refs); business frequency_preference_changed self · touched-before-event · not-applicable
- discovery: 4 aliases
- reviewer: Service class, not security. The confirmation carries the preference centre as its destination. A full opt-out is a no-action outcome for this journey. (changed: journey.contact.defaultPriority, journey.implementation.attributes.required, touch t1, exits, aliases, useCases)

### CON-31 · Permission Validation — migrated (Gate 4; silent lifecycle state)
- entity instance: `person_id + purpose_channel_scope_key` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.rejected=invalid-state, x.active=success
- measurement: journey exit (2 refs); no business outcome
- discovery: 5 aliases
- reviewer: Keyed on the person and the purpose-channel-scope the permission is about. Something that is not a permission is an invalid state, not a success. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### CON-32 · Preference Capture — migrated (Gate 4; silent lifecycle state)
- entity instance: `person_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: x.stored-only=success
- measurement: journey exit-or-handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Accepted as drafted. (changed: aliases, useCases)

### CON-33 · Preference Recalculation — migrated (Gate 4; silent lifecycle state)
- entity instance: `person_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: x.recalculated=success
- measurement: journey exit (1 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies the trigger must not start on. (changed: trigger, aliases, useCases)

### CON-38 · Communication Suppression — migrated (Gate 4; silent lifecycle state)
- entity instance: `person_id + suppression_scope` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- waits → Config: w.release: communication_suppression.release (required) [observation-window, trigger]
- exits classed: x.persistent=suppression, x.released=success
- measurement: journey exit (2 refs); no business outcome
- discovery: 5 aliases
- reviewer: Named the proxies. Keyed on the person and the suppression scope. A released suppression is the journey's success; the persistent one is the suppression state itself. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, trigger, aliases, useCases)

### DEC-184 · More Information Request — migrated (Gate 4; communicating)
- entity instance: `decision_case_id + requirement_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.info: information_requirement.info (required) [attribute-bound, attribute@information_deadline_at]
- exits classed: x.moot=invalid-state, x.closed-noinfo=timeout
- loop budgets: a.request: information_requirement.request_budget (required); a.request-internal: information_requirement.request_internal_budget (required)
- orchestration: notice-then-confirm, 2 touch(es) [t1:persistent, t2:human], no-action 4; contact service/service, cap information_requirement.touches (required) (all), cooldown information_requirement.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business requested_information_received self · touched-before-event · not-applicable
- discovery: 5 aliases
- reviewer: Keyed on the decision case and the requirement. The external and internal requests are the exclusive arms of c.source-type; the external request carries where to provide the item. Requests repeat only within the request budget policy allows, so the local cap is that budget - a required config. The wait is bound to the information deadline. A moot case is an invalid state; closure for want of information is a timeout. (changed: entity.instanceKey, journey.contact.localCap, journey.implementation.attributes.required, touch t1, wait w.info, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): The external request and the internal request are the exclusive arms of c.source-type - who holds the information.

### DEC-267 · Adverse Decision Recovery — migrated (Gate 4; communicating)
- entity instance: `decision_id` · one-active-per-key
- eligibility 5 · suppressions 5 (CCCCC)
- waits → Config: w.remediate: adverse_decision.remediate (required) [attribute-bound, attribute@correction_deadline_at]
- exits classed: x.corrected=success, x.withdrawn=invalid-state, x.reapply=failure, x.final=failure
- orchestration: offer-decide-remind, 5 touch(es) [t1:persistent>in-session, t2←w.remediate after t1:persistent>in-session, t3←w.remediate after t1:persistent>in-session, t4:persistent>in-session, t5:persistent>in-session], no-action 5; contact service/service, cap adverse_decision.touches = 2 [corpus-rule] (all), cooldown adverse_decision.cooldown = none [corpus-rule], competition none
- measurement: journey exit (4 refs); business correction_recorded_complete self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the decision. Correctable, reapplicable and final are the exclusive arms of c.path; the correctable and reapply statements carry their paths, the cleared confirmation and the expiry notice follow the correctable statement and carry none by design. The remediation wait is bound to the correction deadline. At most two touches. A decision that stands is a failure outcome for the subject, stated honestly. Outcome: correction, compared pre/post. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t1, touch t3, touch t4, wait w.remediate, exits, aliases, useCases)
- warnings reviewed: touch_no_destination (t2): The cleared confirmation names what the decision no longer affects; it asks for nothing by design. | simultaneous_review (t1, t4, t5): Correctable, reapplicable and final are the exclusive arms of c.path - exactly one is true.

### DOC-214 · Document Delivery — migrated (Gate 4; communicating)
- entity instance: `document_version_id + recipient_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.distribution: document_distribution.distribution (required) [external-window, previous-touch]
- exits classed: x.distributed=success
- orchestration: single-notice, 1 touch(es) [t1 mandatory:persistent], no-action 3; contact transactional/none, cap document_distribution.touches = 1 [corpus-rule] (non-mandatory), cooldown document_distribution.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (3 refs); business distribution_confirmed self · touched-before-event · not-applicable
- discovery: 5 aliases
- reviewer: Keyed on the version and the recipient. The distribution wait is the delivery mechanism's own window. (changed: entity.instanceKey, journey.implementation.attributes.required, wait w.distribution, aliases, useCases)

### DOC-215 · Signature Reminder — migrated (Gate 3; communicating)
- entity instance: `document_version_id` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- waits → Config: w.signatures: signature.reminder_point = 3 days–5 days [example-only] [reminder-before-attribute, attribute@validity_ends_at]; w.expiry: signature.validity_window = validity_ends_at where defined, otherwise the process's recorded review point [attribute-bound] [attribute-bound, attribute@validity_ends_at]
- exits classed: x.superseded=invalid-state, x.expired=timeout
- loop budgets: a.record-sig: signature.required_signers (required)
- orchestration: offer-decide-remind, 2 touch(es) [t-request mandatory:persistent, t-remind←w.signatures after t-request:persistent], no-action 5; contact transactional/none, cap signature.reminders = 1 [corpus-rule] (non-mandatory), cooldown signature.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business signer_signed self · entered-before-event · not-applicable
- discovery: 6 aliases

### DOC-220 · Document Conflict Review — migrated (Gate 4; communicating)
- entity instance: `document_lineage_id + conflict_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.reconciled=success
- orchestration: single-notice, 1 touch(es) [t1 mandatory:persistent], no-action 4; contact transactional/none, cap document_conflict.touches = 1 [corpus-rule] (non-mandatory), cooldown document_conflict.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the lineage and the conflict. (changed: entity.instanceKey, journey.implementation.attributes.required, aliases, useCases)

### DOC-286 · Document Activation — migrated (Gate 4; communicating)
- entity instance: `document_version_id + person_id` · one-active-per-key
- eligibility 5 · suppressions 5 (CCCCC)
- waits → Config: w.first-use: document_effective.first_use (required) [observation-window, previous-touch]; w.dormancy: document_effective.dormancy (required) [attribute-bound, attribute@effective_period_ends_at]
- exits classed: x.standing=success, x.used=success, x.moot=invalid-state, x.dormant=timeout
- orchestration: offer-decide-remind, 3 touch(es) [t1 mandatory:persistent>in-session, t2 mandatory:persistent>in-session, t3←w.first-use after t1:persistent>in-session], no-action 5; contact transactional/none, cap document_effective.touches = 1 [corpus-rule] (non-mandatory), cooldown document_effective.cooldown = none [corpus-rule], competition none
- measurement: journey exit (4 refs); business capability_first_used self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: The actionable and standing notices are the exclusive arms of c.usable; the actionable notice and the reminder carry the first action. The reminder is discretionary. The dormancy wait is bound to the effective period. Ended without use is an invalid state; dormant is a timeout. Outcome compared pre/post. (changed: journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t1, touch t3, wait w.dormancy, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): The actionable and standing notices are the exclusive arms of c.usable - whether the entitlement is exercised or held.

### FBK-41 · Feedback Request — migrated (Gate 3; communicating)
- entity instance: `person_id + experience_ref` · one-active-per-key
- eligibility 5 · suppressions 5 (CCCCC)
- waits → Config: w.completion: feedback_request.completion_horizon (required) [external-window, trigger]; w.response: feedback_request.response_window = 3 days–7 days [example-only] [response-window, previous-touch]
- exits classed: x.never-completed=invalid-state, x.deferred=suppression, x.duplicate=suppression, x.not-now=no-action, x.received=success, x.no-response=timeout
- orchestration: single-notice, 1 touch(es) [t1:in-session>persistent>low-friction], no-action 5; contact lifecycle/lifecycle, cap feedback_request.touches = 1 [corpus-rule] (all), cooldown feedback_request.ask_frequency = 30 days–90 days [example-only], competition outbound-ask
- measurement: journey exit (6 refs); business feedback_submitted self · touched-before-event · none
- discovery: 7 aliases

### FBK-42 · Advocacy Request — migrated (Gate 4; communicating)
- entity instance: `account_id + relationship_id` · one-active-per-key
- eligibility 3 · suppressions 6 (CCCCCC)
- waits → Config: w.response: advocacy_eligibility.response (required) [response-window, previous-touch]
- exits classed: x.suppressed=suppression, x.delay=no-action, x.contributed=success, x.declined=no-action, x.no-response=timeout
- orchestration: single-notice, 2 touch(es) [t1:persistent>in-session>low-friction, t2:persistent>in-session>low-friction], no-action 6; contact lifecycle/lifecycle, cap advocacy_eligibility.touches = 2 [corpus-rule] (all), cooldown advocacy_eligibility.cooldown (required), competition outbound-ask
- measurement: journey exit-or-handoff (6 refs); business advocacy_action_taken self · touched-before-event · none
- discovery: 6 aliases
- reviewer: The light and heavy asks are the exclusive arms of c.type. The heavy ask carries its contribution route. Not-yet-eligible is a no-action state. One ask, no follow-up. (changed: journey.orchestration.strategy, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t2, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): The light and heavy asks are the exclusive arms of c.type - the size of request the evidence supports.

### FBK-43 · Feedback Follow-Up — migrated (Gate 3; communicating)
- entity instance: `feedback_id` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- waits → Config: w.outcome: feedback.obligation_sla (required) [decision-sla, trigger]; w.followup: feedback.promise_window = 7 days–14 days [example-only] [response-window, attribute@promised_followup_at]
- exits classed: x.evidence=success, x.attached=suppression, x.acknowledged=success, x.open=failure, x.stored=success, x.closed=success
- orchestration: single-notice, 3 touch(es) [t-comment:persistent>in-session, t-positive:persistent>in-session, t-negative:persistent>in-session], no-action 5; contact service/service, cap feedback.touches = 1 [corpus-rule] (all), cooldown feedback.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (11 refs); business work_item_outcome_recorded self · entered-before-event · not-applicable
- discovery: 7 aliases

### FBK-46 · Complaint Resolution — migrated (Gate 3; communicating)
- entity instance: `issue_id` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- waits → Config: w.resolution: complaint.sla_threshold (required) [decision-sla, trigger]; w.confirm: complaint.closure_window = 5 days–10 days [example-only] [response-window, previous-touch]
- exits classed: x.closed=success, x.closed-unconfirmed=success
- loop budgets: a.escalate: complaint.escalation_levels = 3 [corpus-rule]; a.request-confirmation: complaint.reopen_budget = 2 [example-only]
- orchestration: human-escalation-ladder, 1 touch(es) [t1←w.resolution:persistent>in-session], no-action 5; contact service/service, cap complaint.confirmation_asks = 2 [example-only] (all), cooldown complaint.cooldown = none [corpus-rule], competition retention-outreach
- measurement: journey exit-or-handoff (4 refs); business resolution_confirmed self · entered-before-event · not-applicable
- discovery: 7 aliases

### FBK-47 · Appeal Review — migrated (Gate 4; communicating)
- entity instance: `appeal_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.review: appeal_and.review (required) [attribute-bound, attribute@appeal_deadline_at]; w.more-info: appeal_and.more_info (required) [attribute-bound, attribute@appeal_deadline_at]
- exits classed: x.rejected=invalid-state, x.concluded=success
- loop budgets: a.request-more-info: appeal_and.request_more_info_budget (required)
- orchestration: offer-decide-remind, 3 touch(es) [t1:persistent, t2←w.review:persistent, t3←w.review:persistent], no-action 3; contact service/service, cap appeal_and.touches = 3 [corpus-rule] (all), cooldown appeal_and.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (3 refs); business review_concluded self · entered-before-event · not-applicable
- discovery: 5 aliases
- reviewer: Keyed on the appeal. An appeal is service communication. Each touch carries the thing it points at; the information request and the outcome are the exclusive arms of c.outcome. Both waits are bound to the appeal deadline. An ineligible appeal is an invalid state. One appeal, no cooldown. Outcome: the review concluding. (changed: entity.instanceKey, journey.contact.defaultPriority, journey.contact.pressureClass, journey.contact.cooldown, journey.measurement.businessOutcome, journey.implementation.attributes.required, touch t1, touch t2, touch t3, wait w.review, wait w.more-info, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t2, t3): The information request and the outcome statement are the exclusive arms of c.outcome (MORE_INFORMATION_REQUIRED vs a conclusion).

### FBK-48 · Declared Context Recalculation — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + person_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: x.applied=success, x.stored=success
- measurement: journey exit (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Accepted as drafted. (changed: aliases, useCases)

### FBK-49 · Missing Information Reminder — migrated (Gate 4; communicating)
- entity instance: `blocked_entity_id + requirement_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.received: missing_critical.received (required) [response-window, previous-touch]
- exits classed: x.resumed=success, x.alternate=invalid-state, x.abandoned=failure
- orchestration: notice-then-confirm, 2 touch(es) [t1:persistent>in-session, t2:human], no-action 3; contact service/service, cap missing_critical.touches = 2 [corpus-rule] (all), cooldown missing_critical.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business requested_information_received self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the blocked entity and the requirement. Service class. The customer request and the internal request are the exclusive arms of c.provider; the customer request carries where to provide the item. Exit classes corrected: a satisfied requirement is the success, an alternate route is an invalid state for this requirement. Per requirement, no cooldown. The wait is a response window. (changed: entity.instanceKey, journey.contact.defaultPriority, journey.contact.pressureClass, journey.contact.cooldown, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t1, wait w.received, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): The customer request and the internal request are the exclusive arms of c.provider - who can supply the item.

### FBK-50 · Relationship State Reassessment — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + relationship_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.owned=success, x.evidence=success, x.reassessed=success, x.no-label=success
- measurement: journey exit (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Accepted as drafted. (changed: aliases, useCases)

### FIN-131 · Financial Obligation Tracking — migrated (Gate 4; silent lifecycle state)
- entity instance: `obligation_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.obligation: financial_obligation.obligation (required) [response-window, trigger]
- exits classed: x.adjusted=success, x.aged=timeout
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: An obligation outstanding past its horizon is a timeout outcome. (changed: exits, aliases, useCases)

### FIN-134 · Payment Failure Recovery — migrated (Gate 2; communicating)
- entity instance: `obligation_id` · one-active-per-key
- eligibility 5 · suppressions 5 (CCCCC)
- waits → Config: w.alternate-choice: payment.alternate_choice_window = 3 days–7 days [example-only] [response-window, previous-touch]; w.recovery: payment.reminder_point = 2 days–3 days [example-only] [reminder-before-attribute, attribute@consequence_date]; w.final: payment.recovery_window (required) [recovery-window, trigger]
- exits classed: x.recovered=success
- orchestration: notice-then-confirm, 4 touch(es) [t1 mandatory:in-session>persistent, t1-alt mandatory:in-session>persistent, t2←w.recovery after t1:persistent>urgent, t3 after t1 mandatory:persistent>in-session], no-action 5; contact transactional/none, cap payment.discretionary_touches = 1 [corpus-rule] (non-mandatory), cooldown payment.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business obligation_satisfied handoff-chain→TIM-65 · entered-before-event · pre-post
- discovery: 5 aliases

### FIN-136 · Balance Reconciliation — migrated (Gate 4; silent lifecycle state)
- entity instance: `obligation_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.partial=success, x.satisfied=success
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. A partial satisfaction is a correct reconciliation with the remainder explicit. (changed: exits, trigger, aliases, useCases)

### FIN-137 · Refund Request — migrated (Gate 4; communicating)
- entity instance: `refund_request_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.decision: refund_request.decision (required) [decision-sla, trigger]
- exits classed: x.rejected=failure
- orchestration: notice-then-confirm, 5 touch(es) [t1 mandatory:persistent, t2 mandatory:persistent, t3 after t4 mandatory:persistent, t4 mandatory:human, t5 mandatory:human], no-action 3; contact transactional/none, cap refund_request.touches = 0 [corpus-rule] (non-mandatory), cooldown refund_request.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the refund request. Every touch is mandatory (transactional), so the discretionary cap is zero. Rejection, review and approval are the exclusive arms of c.eligible; the review acknowledgement follows the human review step and carries no route by design. The decision SLA runs from the request. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.rule, journey.implementation.attributes.required, touch t3, wait w.decision, aliases, useCases)
- warnings reviewed: simultaneous_review (t2, t3, t4, t5): Rejection notice, review acknowledgement, human review and approval share c.eligible's prerequisites: rejection, review and approval are its exclusive arms, and the acknowledgement follows the review step. | touch_no_destination (t3): The under-review acknowledgement states a state without implying an outcome; it asks for nothing by design.

### FUL-141 · Fulfillment Request Validation — migrated (Gate 4; silent lifecycle state)
- entity instance: `request_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.dependency: fulfillment_request.dependency (required) [observation-window, trigger]
- exits classed: x.rejected=invalid-state, x.lapsed=timeout
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the request. A rejected request is an invalid state, not a failure of validation. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### FUL-142 · Fulfillment Allocation — migrated (Gate 4; silent lifecycle state)
- entity instance: `obligation_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.capacity: fulfillment_availability.capacity (required) [observation-window, trigger]
- exits classed: 
- measurement: journey handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the obligation. Pure router by design: resources exist or they do not. (changed: entity.instanceKey, journey.implementation.attributes.required, trigger, aliases, useCases)
- warnings reviewed: router_journey: Allocation decides whether resources exist and hands to reservation or to cancellation reconciliation.

### FUL-143 · Resource Reservation — migrated (Gate 4; silent lifecycle state)
- entity instance: `allocation_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.allocation: resource_allocation.allocation (required) [attribute-bound, attribute@reservation_expires_at]
- exits classed: x.consumed=success, x.released=success, x.expired=timeout
- measurement: journey exit-or-handoff (5 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the allocation. The wait is bound to the reservation's own expiry. (changed: entity.instanceKey, journey.implementation.attributes.required, wait w.allocation, trigger, aliases, useCases)

### FUL-144 · Fulfillment Execution — migrated (Gate 4; silent lifecycle state)
- entity instance: `obligation_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.execution: fulfillment_execution.execution (required) [observation-window, trigger]
- exits classed: x.partial=success
- measurement: journey exit-or-handoff (6 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the obligation. (changed: entity.instanceKey, journey.implementation.attributes.required, trigger, aliases, useCases)

### FUL-145 · Fulfillment Exception Recovery — migrated (Gate 4; silent lifecycle state)
- entity instance: `exception_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.approval: fulfillment_exception.approval (required) [response-window, trigger]
- exits classed: 
- measurement: journey handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the exception. Pure router by design: every resolution belongs to another journey. (changed: entity.instanceKey, journey.implementation.attributes.required, aliases, useCases)
- warnings reviewed: router_journey: Every resolution of an exception belongs to execution, delay handling or cancellation reconciliation.

### FUL-146 · Delivery Delay Alert — migrated (Gate 4; communicating)
- entity instance: `obligation_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.decision: fulfillment_delay.decision (required) [response-window, previous-touch]; w.resume: fulfillment_delay.resume (required) [observation-window, trigger]
- exits classed: 
- orchestration: offer-decide-remind, 3 touch(es) [t1:persistent>urgent, t2:persistent>urgent, t3:persistent>urgent], no-action 4; contact service/service, cap fulfillment_delay.touches = 2 [corpus-rule] (all), cooldown fulfillment_delay.cooldown = none [corpus-rule], competition none
- measurement: journey handoff (4 refs); business fulfillment_resumed_or_completed self · touched-before-event · not-applicable
- discovery: 5 aliases
- reviewer: The no-choice update and the offer are the exclusive arms of c.choice; the offer carries the choices. At most two touches. Pure router by design: every ending belongs to execution, exception, cancellation or ownership. Outcome: fulfilment resuming. (changed: journey.orchestration.strategy, journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.measurement.businessOutcome, journey.implementation.attributes.required, touch t3, aliases, useCases)
- warnings reviewed: router_journey: A delay ends in resumption, an exception, a cancellation or ownership escalation - all owned elsewhere. | simultaneous_review (t2, t3): The no-choice update and the offer are the exclusive arms of c.choice.

### FUL-147 · Delivery Outcome Tracking — migrated (Gate 4; silent lifecycle state)
- entity instance: `dispatch_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.delivery: dispatch_and.delivery (required) [external-window, trigger]
- exits classed: 
- measurement: journey handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the dispatch. Pure router by design: the executor's outcome decides the next journey. (changed: entity.instanceKey, journey.implementation.attributes.required, aliases, useCases)
- warnings reviewed: router_journey: The executor's report decides the next journey: confirmation, failed-attempt recovery or delay.

### FUL-148 · Failed Delivery Recovery — migrated (Gate 4; communicating)
- entity instance: `delivery_attempt_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.correction: delivery_attempt.correction (required) [response-window, previous-touch]; w.route-choice: delivery_attempt.route_choice (required) [response-window, previous-touch]
- exits classed: 
- orchestration: notice-then-confirm, 2 touch(es) [t1:persistent>urgent, t2:persistent>urgent], no-action 4; contact service/service, cap delivery_attempt.touches = 2 [corpus-rule] (all), cooldown delivery_attempt.cooldown = none [corpus-rule], competition none
- measurement: journey handoff (3 refs); business delivery_correction_provided self · touched-before-event · not-applicable
- discovery: 5 aliases
- reviewer: Keyed on the delivery attempt. The correction request and the alternatives offer each carry their route. Both waits are response windows from the touch. Pure router by design. (changed: entity.instanceKey, journey.implementation.attributes.required, touch t1, touch t2, wait w.correction, wait w.route-choice, aliases, useCases)
- warnings reviewed: router_journey: A failed attempt ends in a retry dispatch, a return or an exception - all owned elsewhere.

### FUL-149 · Delivery Acceptance Finalization — migrated (Gate 4; silent lifecycle state)
- entity instance: `obligation_id + delivery_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.acceptance: delivery_acceptance.acceptance (required) [attribute-bound, attribute@acceptance_window_ends_at]; w.window: delivery_acceptance.window (required) [attribute-bound, attribute@issue_window_ends_at]
- exits classed: x.finalized=success
- measurement: journey exit-or-handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the obligation and the delivery. Both waits are bound to the policy windows. (changed: entity.instanceKey, journey.implementation.attributes.required, wait w.acceptance, wait w.window, trigger, aliases, useCases)

### FUL-150 · Fulfillment Cancellation Reconciliation — migrated (Gate 4; silent lifecycle state)
- entity instance: `obligation_id` · one-active-per-key
- eligibility 3 · suppressions 6 (CCCCCC)
- exits classed: x.cancelled=success
- measurement: journey exit-or-handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: A completed reconciliation is the success of this journey. (changed: exits, aliases, useCases)

### FUL-265 · Delivery Tracking — migrated (Gate 4; communicating)
- entity instance: `obligation_id + person_id` · one-active-per-key
- eligibility 4 · suppressions 5 (CCCCC)
- waits → Config: w.delivery: dispatch_to.delivery (required) [external-window, previous-touch]; w.acceptance: dispatch_to.acceptance (required) [attribute-bound, attribute@acceptance_window_ends_at]
- exits classed: x.unresolved=failure, x.delivered=success, x.accepted=success, x.finalized=timeout
- orchestration: offer-decide-remind, 4 touch(es) [t1:persistent>urgent, t2←w.delivery after t1:persistent>urgent, t3←w.delivery after t1:persistent>urgent, t4 after t3:persistent>urgent], no-action 5; contact service/service, cap dispatch_to.touches = 3 [corpus-rule] (all), cooldown dispatch_to.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (5 refs); business delivery_accepted self · touched-before-event · not-applicable
- discovery: 5 aliases
- reviewer: The acceptance request follows the arrival confirmation directly, not through the delivery wait - the drafted gate was wrong. The arrival confirmation carries no route by design; the acceptance request carries the accept-or-raise route. The delivery wait is the executor's own window; the acceptance wait is bound to the acceptance window. The longest path sends three touches. Outcome: acceptance. (changed: journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.measurement.businessOutcome, journey.implementation.attributes.required, touch t3, touch t4, wait w.delivery, wait w.acceptance, aliases, useCases)
- warnings reviewed: touch_no_destination (t3): The arrival confirmation states the evidence of delivery and asks for nothing; the acceptance request that may follow carries the route.

### FUL-276 · Substitution Approval — migrated (Gate 4; communicating)
- entity instance: `obligation_id + substitution_offer_id` · one-active-per-key
- eligibility 5 · suppressions 5 (CCCCC)
- waits → Config: w.decision: substitution_offer.decision (required) [reminder-before-attribute, attribute@offer_closes_at]; w.final: substitution_offer.final (required) [attribute-bound, attribute@offer_closes_at]
- exits classed: x.accepted=success, x.declined=failure, x.lapsed=timeout
- orchestration: offer-decide-remind, 5 touch(es) [t1:persistent>urgent, t2←w.decision after t1:persistent>urgent, t3←w.decision:persistent>urgent, t4←w.decision:persistent>urgent, t5←w.final:persistent>urgent], no-action 5; contact service/service, cap substitution_offer.touches = 3 [corpus-rule] (all), cooldown substitution_offer.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business substitute_approved self · touched-before-event · not-applicable
- discovery: 4 aliases
- reviewer: Keyed on the obligation and the offer. The offer and the reminder carry the accept-or-decline route; the two confirmations are the exclusive arms of c.answer and the lapse notice closes the offer - none of the three carries a route by design. Both waits are bound to the offer's closing date. A decline is a failure of the substitution, not a no-action. The longest path sends three touches. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.implementation.attributes.required, touch t1, touch t2, wait w.decision, wait w.final, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t3, t4): The accept and decline confirmations are the exclusive arms of c.answer. | touch_no_destination (t3): A confirmation or lapse notice closes the offer and asks for nothing by design; the offer and reminder carried the route. | touch_no_destination (t4): A confirmation or lapse notice closes the offer and asks for nothing by design; the offer and reminder carried the route. | touch_no_destination (t5): A confirmation or lapse notice closes the offer and asks for nothing by design; the offer and reminder carried the route.

### IDN-270 · Account Recovery — migrated (Gate 4; communicating)
- entity instance: `recovery_case_id` · one-active-per-key
- eligibility 5 · suppressions 5 (CCCCC)
- waits → Config: w.proof: account_recovery.proof (required) [reminder-before-attribute, attribute@recovery_window_ends_at]; w.final: account_recovery.final (required) [attribute-bound, attribute@recovery_window_ends_at]
- exits classed: x.restored=success, x.moot=success, x.closed=timeout
- orchestration: offer-decide-remind, 3 touch(es) [t1 mandatory:persistent>urgent, t2←w.proof after t1 mandatory:persistent>urgent, t3←w.proof after t1:persistent>urgent], no-action 5; contact security/none, cap account_recovery.touches = 1 [corpus-rule] (non-mandatory), cooldown account_recovery.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business proof_of_control_provided self · touched-before-event · not-applicable
- discovery: 5 aliases
- reviewer: Keyed on the recovery case. The final wait routes through c.proof so the restoration confirmation is a decision, not a send on the success event. The route issue carries the recovery route; the confirmation carries none by design; the reminder is discretionary. Both waits are bound to the recovery window. (changed: entity.instanceKey, journey.implementation.attributes.required, touch t1, touch t3, wait w.proof, wait w.final, aliases, useCases)
- warnings reviewed: touch_no_destination (t2): The restoration confirmation asks for nothing; it names what was invalidated and carries no route by design.

### IDN-271 · Account Security Alert — migrated (Gate 3; communicating)
- entity instance: `account_id + incident_id` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- waits → Config: w.verify: security_alert.review_point (required) [decision-sla, trigger]
- exits classed: x.withheld=no-action, x.cleared=success, x.open=timeout
- orchestration: notice-then-confirm, 4 touch(es) [t-contained mandatory:persistent>urgent, t-watch mandatory:persistent>urgent, t-cleared←w.verify mandatory:persistent>urgent, t-standing←w.verify mandatory:persistent>urgent], no-action 5; contact security/none, cap security_alert.discretionary_touches = 0 [corpus-rule] (non-mandatory), cooldown security_alert.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business activity_confirmed_own self · entered-before-event · not-applicable
- discovery: 6 aliases

### IDN-81 · Identity Verification — migrated (Gate 4; communicating)
- entity instance: `claim_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.evidence: identity_claim.evidence (required) [response-window, previous-touch]
- exits classed: x.expired=timeout, x.verified=success
- loop budgets: a.validate: identity_claim.validate_budget (required)
- orchestration: notice-then-confirm, 2 touch(es) [t1 mandatory:in-session>persistent, t2←w.evidence after t1 mandatory:in-session>persistent], no-action 4; contact security/none, cap identity_claim.touches = 0 [corpus-rule] (non-mandatory), cooldown identity_claim.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business verification_evidence_received self · touched-before-event · not-applicable
- discovery: 5 aliases
- reviewer: Keyed on the claim. Both requests carry the evidence route and are mandatory; the follow-up request follows the first through the evidence wait. Every touch is mandatory, so the discretionary cap is zero. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.rule, journey.implementation.attributes.required, touch t1, touch t2, aliases, useCases)

### IDN-84 · Verification Recovery — migrated (Gate 4; communicating)
- entity instance: `verification_instance_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.retry=success, x.terminal=failure
- orchestration: notice-then-confirm, 2 touch(es) [t1 mandatory:in-session>persistent, t2 mandatory:in-session>persistent], no-action 4; contact security/none, cap verification_failure.touches = 1 [corpus-rule] (non-mandatory), cooldown verification_failure.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the verification instance. Each explanation carries the route it names. A terminal basis is a failure outcome. (changed: entity.instanceKey, journey.implementation.attributes.required, touch t1, touch t2, exits, aliases, useCases)

### IDN-85 · Login Verification — migrated (Gate 4; communicating)
- entity instance: `session_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.auth: authentication_challenge.auth (required) [response-window, previous-touch]
- exits classed: x.satisfied=success, x.timeout=timeout, x.authenticated=success
- orchestration: single-notice, 1 touch(es) [t1 mandatory:urgent>low-friction>persistent], no-action 4; contact security/none, cap authentication_challenge.touches = 0 [corpus-rule] (non-mandatory), cooldown authentication_challenge.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (5 refs); business authentication_succeeded self · touched-before-event · not-applicable
- discovery: 6 aliases
- reviewer: Keyed on the authentication session. The challenge is mandatory, so the discretionary cap is zero. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.rule, journey.implementation.attributes.required, aliases, useCases)

### IDN-87 · Authentication Risk Assessment — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: x.normal=no-action
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: An ordinary failure pattern is a no-action outcome. (changed: exits, aliases, useCases)

### IDN-88 · Account Recovery Verification — migrated (Gate 4; silent lifecycle state)
- entity instance: `recovery_case_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.proof: account_recovery.proof (required) [attribute-bound, attribute@recovery_window_ends_at]
- exits classed: x.expired=timeout, x.recovered=success, x.more=no-action, x.denied=failure
- measurement: journey exit-or-handoff (6 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the recovery case. The proof wait is bound to the recovery window. Further evidence allowed is a no-action state of this instance. (changed: entity.instanceKey, journey.implementation.attributes.required, wait w.proof, exits, trigger, aliases, useCases)

### IDN-89 · Identity Attribute Update — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + attribute_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.verification: identity_attribute.verification (required) [response-window, trigger]
- exits classed: x.not-applied=invalid-state, x.rejected=invalid-state, x.reconciled=success, x.updated=success
- measurement: journey exit (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the account and the attribute. A change not verified or invalid leaves the attribute as it was: invalid-state outcomes. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, trigger, aliases, useCases)

### IDN-90 · Account Compromise Recovery — migrated (Gate 4; silent lifecycle state)
- entity instance: `incident_id` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- waits → Config: w.resolution: suspected_account.resolution (required) [decision-sla, trigger]
- exits classed: x.continued=timeout
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the incident. An SLA passing without a conclusion is a timeout outcome. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### INC-254 · Incident Update — migrated (Gate 4; communicating)
- entity instance: `incident_id + recipient_cohort_id + state_change_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.no-send=no-action, x.updated=success, x.closed-comms=success
- orchestration: single-notice, 1 touch(es) [t1:persistent>in-session], no-action 4; contact service/service, cap incident_communication.touches = 1 [corpus-rule] (all), cooldown incident_communication.cooldown = none [corpus-rule], competition none
- measurement: journey exit (3 refs); no business outcome
- discovery: 5 aliases
- reviewer: Keyed on the incident, the cohort and the state change. Nothing material to say is a no-action outcome. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### INT-269 · Integration Recovery — migrated (Gate 4; communicating)
- entity instance: `connection_id + failure_id` · one-active-per-key
- eligibility 5 · suppressions 4 (CCCC)
- waits → Config: w.revalidate: integration_reconnect.revalidate (required) [response-window, previous-touch]
- exits classed: x.informed=success, x.restored=success, x.removed=invalid-state, x.unreconnected=timeout
- orchestration: offer-decide-remind, 4 touch(es) [t1:persistent>in-session, t2:persistent>in-session, t3:persistent>in-session, t4←w.revalidate:persistent>in-session], no-action 4; contact service/service, cap integration_reconnect.touches = 2 [corpus-rule] (all), cooldown integration_reconnect.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (5 refs); business connection_revalidated self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the connection and the failure. The partial and total notices are the exclusive arms of c.scope and both carry the re-authorisation step; the restoration confirmation carries none by design. The revalidation wait is a response window from the notice. Informed-only is a correct outcome; a removed connection is an invalid state. At most two touches. Outcome: revalidation, compared pre/post. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.measurement.businessOutcome, journey.implementation.attributes.required, touch t2, touch t3, wait w.revalidate, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t2, t3): Partial and total notices are the exclusive arms of c.scope - how much of the connection is affected. | touch_no_destination (t4): The restoration confirmation names what came back and carries no route by design.

### INT-278 · Integration Setup — migrated (Gate 4; communicating)
- entity instance: `connection_attempt_id` · one-active-per-key
- eligibility 4 · suppressions 5 (CCCCC)
- waits → Config: w.retry: integration_setup.retry (required) [observation-window, previous-touch]
- exits classed: x.active=success, x.unresolved=failure, x.abandoned=timeout
- loop budgets: a.fix-auth: integration_setup.fix_auth_budget (required); a.fix-scope: integration_setup.fix_scope_budget (required); a.fix-capability: integration_setup.fix_capability_budget (required)
- orchestration: notice-then-confirm, 5 touch(es) [t1:persistent>in-session, t2:persistent>in-session, t3:persistent>in-session, t4:persistent>in-session, t5:persistent>in-session], no-action 5; contact service/service, cap integration_setup.touches (required) (all), cooldown integration_setup.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 5 aliases
- reviewer: Keyed on the connection attempt. The active confirmation and the generic failure are the exclusive arms of c.outcome; the three stage fixes are the exclusive arms of c.stage and each carries its fix; the confirmation carries none by design. Stage messages repeat through retries, so the local cap is the retry budget the company fixes - a required config. Still-not-connected is a failure; abandonment is a timeout. (changed: entity.instanceKey, journey.contact.localCap, journey.implementation.attributes.required, touch t2, touch t3, touch t4, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t5): The active confirmation and the generic failure are the exclusive arms of c.outcome. | simultaneous_review (t2, t3, t4): The credential, permission and capability fixes are the exclusive arms of c.stage - exactly one stage failed. | touch_no_destination (t1): The active confirmation names the validated capabilities and carries no route by design.

### REL-100 · Orphan Relationship Recovery — migrated (Gate 4; silent lifecycle state)
- entity instance: `entity_ref + relationship_type` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.restore: orphaned_entity.restore (required) [decision-sla, trigger]
- exits classed: x.resumed=success, x.terminal=timeout
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the entity and the relationship type it is missing. A policy end state after the SLA is a timeout outcome. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, trigger, aliases, useCases)

### REL-284 · Invitation Reminder — migrated (Gate 4; communicating)
- entity instance: `invitation_id` · one-active-per-key
- eligibility 5 · suppressions 5 (CCCCC)
- waits → Config: w.response: relationship_invitation.response (required) [reminder-before-attribute, attribute@expires_at]; w.final: relationship_invitation.final (required) [attribute-bound, attribute@expires_at]
- exits classed: x.active=success, x.closed=failure, x.expired=timeout
- orchestration: offer-decide-remind, 4 touch(es) [t1:persistent>in-session, t2:persistent>in-session, t3←w.response:persistent>in-session, t4←w.response:persistent>in-session], no-action 5; contact service/service, cap relationship_invitation.touches = 3 [corpus-rule] (all), cooldown relationship_invitation.cooldown = none [corpus-rule], competition none
- measurement: journey exit (3 refs); business invitation_accepted self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the invitation. The known-holder and new-counterparty invitations are the exclusive arms of c.known and both carry the acceptance route; the confirmation carries none by design. Both waits are bound to the invitation's expiry. The longest path sends three touches. A declined invitation is a failure of the ask, not a no-action. Outcome: acceptance, compared pre/post. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t1, touch t2, touch t4, wait w.final, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): The known-holder and new-counterparty invitations are the exclusive arms of c.known. Never both. | touch_no_destination (t3): The activation confirmation to both sides names scope and direction and carries no route by design.

### REL-91 · Relationship Validation — migrated (Gate 4; silent lifecycle state)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.pending=no-action, x.rejected=invalid-state, x.active=success
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Pending evidence is a no-action state; a rejected link is an invalid state, not a failure of this journey. (changed: exits, aliases, useCases)

### REL-92 · Relationship Impact Recalculation — migrated (Gate 4; silent lifecycle state)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- exits classed: x.recorded=success, x.recalculated=success
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Accepted as drafted. (changed: aliases, useCases)

### REL-93 · Entity Relationship End Reconciliation — migrated (Gate 4; silent lifecycle state)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.ended=success
- measurement: journey exit-or-handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Accepted as drafted. (changed: aliases, useCases)

### REL-94 · Role Authority Update — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + member_id + role_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.applied=success
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the account, the member and the role. (changed: entity.instanceKey, journey.implementation.attributes.required, trigger, aliases, useCases)

### REM-151 · Post-Purchase Issue Recovery — migrated (Gate 4; communicating)
- entity instance: `issue_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.attached=suppression, x.no-defect=no-action
- orchestration: single-notice, 1 touch(es) [t1:persistent], no-action 4; contact service/service, cap post_completion.touches = 1 [corpus-rule] (all), cooldown post_completion.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 5 aliases
- reviewer: Keyed on the issue. Attachment to an existing case is a suppression; no remedy owed is a no-action outcome. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### REM-152 · Return Request — migrated (Gate 4; communicating)
- entity instance: `return_request_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.decision: return_authorization.decision (required) [decision-sla, trigger]
- exits classed: x.rejected=failure
- orchestration: notice-then-confirm, 3 touch(es) [t1:persistent, t2:persistent, t3:human], no-action 3; contact service/service, cap return_authorization.touches = 2 [corpus-rule] (all), cooldown return_authorization.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (5 refs); no business outcome
- discovery: 5 aliases
- reviewer: Keyed on the return request. Rejection, authorisation and review are the exclusive arms of c.eligible; the authorisation carries the return instructions. At most two touches reach the requester. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.implementation.attributes.required, touch t2, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2, t3): Rejection notice, authorisation notice and human review are the exclusive arms of c.eligible.

### REM-156 · Corrective Reperformance — migrated (Gate 4; silent lifecycle state)
- entity instance: `correction_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.correction: correction_reperformance.correction (required) [attribute-bound, attribute@correction_deadline_at]
- exits classed: 
- measurement: journey handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the correction. The wait is bound to the correction's deadline. Pure router by design. (changed: entity.instanceKey, journey.implementation.attributes.required, wait w.correction, trigger, aliases, useCases)
- warnings reviewed: router_journey: A correction ends in verification, an alternative remedy or ownership escalation.

### REM-157 · Remedy Confirmation — migrated (Gate 4; communicating)
- entity instance: `issue_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.selection: remedy_selection.selection (required) [response-window, previous-touch]
- exits classed: x.no-remedy=success
- orchestration: notice-then-confirm, 2 touch(es) [t1:persistent>in-session, t2:persistent>in-session], no-action 4; contact service/service, cap remedy_selection.touches = 2 [corpus-rule] (all), cooldown remedy_selection.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (5 refs); business remedy_selected self · touched-before-event · not-applicable
- discovery: 5 aliases
- reviewer: Keyed on the issue. The options message carries the options; the no-remedy statement carries the appeal route it names. The selection wait is a response window from the touch. (changed: entity.instanceKey, journey.implementation.attributes.required, touch t1, touch t2, wait w.selection, aliases, useCases)

### RET-21 · Engagement Reclassification — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + relationship_id` · one-active-per-key
- eligibility 4 · suppressions 3 (CCC)
- exits classed: x.updated=success
- measurement: journey exit-or-handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Accepted as drafted; aliases named. (changed: aliases, useCases)

### RET-22 · Usage Gap Assessment — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + use_case_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: x.normal-quiet=no-action, x.observe=no-action
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 5 aliases
- reviewer: Keyed on the account and the use-case the expectation belongs to. Both exits are no-action states: a miss with nothing behind it is not risk. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### RET-23 · Health Deterioration Diagnosis — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + relationship_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.diagnostic: health_deterioration.diagnostic (required) [observation-window, trigger]
- exits classed: x.need-changed=success, x.cause-found=success, x.unexplained=timeout
- measurement: journey exit-or-handoff (10 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the relationship the health state is held against. An unexplained deterioration at the end of the diagnostic window is a timeout. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### RET-24 · Churn Risk Escalation — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + risk_episode_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.monitor=no-action
- orchestration: single-notice, 1 touch(es) [t1:human], no-action 4; contact retention/none, cap churn_risk.touches = 1 [corpus-rule] (all), cooldown churn_risk.cooldown = none [corpus-rule], competition retention-outreach
- measurement: journey exit-or-handoff (5 refs); no business outcome
- discovery: 5 aliases
- reviewer: A task to the account owner is not customer pressure: retention priority, pressure class none. Per risk episode, so no cooldown. Keyed on the account and the episode. Monitoring is a no-action state. (changed: entity.instanceKey, journey.contact.defaultPriority, journey.contact.pressureClass, journey.contact.cooldown, journey.implementation.attributes.required, exits, aliases, useCases)

### RET-26 · Service Recovery — migrated (Gate 3; communicating)
- entity instance: `person_id + failure_ref` · one-active-per-key
- eligibility 4 · suppressions 5 (CCCCC)
- exits classed: x.defer=suppression, x.silent=no-action, x.acknowledged=success
- orchestration: single-notice, 1 touch(es) [t1:persistent>low-friction], no-action 5; contact service/service, cap service_recovery.touches = 1 [corpus-rule] (all), cooldown service_recovery.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (5 refs); no business outcome
- discovery: 6 aliases

### RET-27 · Recovery Stability Check — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + person_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.stability: recovery_observation.stability (required) [observation-window, trigger]
- exits classed: 
- measurement: journey handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Pure router by design: sustained recovery goes to the lifecycle owner, a relapse back to diagnosis. Accepted otherwise. (changed: aliases, useCases)
- warnings reviewed: router_journey: The stability check owns only the observation: sustained recovery hands to the lifecycle owner, relapse hands back to diagnosis.

### RET-28 · Cancellation Save — migrated (Gate 3; communicating)
- entity instance: `person_id + relationship_id + intent_id` · one-active-per-key
- eligibility 4 · suppressions 5 (CCCCC)
- waits → Config: w.answer: cancellation_save.answer_window = the end of the session or conversation in which the question was put [attribute-bound] [attribute-bound, previous-touch]; w.decision: cancellation_save.intent_window = 7 days–14 days [example-only] [observation-window, trigger]
- exits classed: x.lapsed=timeout
- orchestration: offer-decide-remind, 2 touch(es) [t-ask:in-session>persistent, t-offer:in-session>persistent], no-action 5; contact retention/lifecycle, cap cancellation_save.touches = 2 [corpus-rule] (all), cooldown cancellation_save.cooldown = 30 days–90 days [example-only], competition retention-outreach
- measurement: journey exit-or-handoff (3 refs); business cancellation_flow_abandoned self · touched-before-event · pre-post
- discovery: 6 aliases

### RET-29 · Cancellation Wind-Down — migrated (Gate 4; silent lifecycle state)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.former=success, x.wind-down=success
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the relationship that ended. Wind-down until the end date is a correct outcome of this journey, not an invalid state. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### RET-30 · Retention Offer Follow-Up — migrated (Gate 4; communicating)
- entity instance: `account_id + retention_episode_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.outcome: retention_intervention.outcome (required) [response-window, trigger]
- exits classed: x.declined=no-action, x.cooldown=timeout
- orchestration: single-notice, 1 touch(es) [t1←w.outcome:persistent>in-session], no-action 4; contact lifecycle/lifecycle, cap retention_intervention.touches = 1 [corpus-rule] (all), cooldown retention_intervention.cooldown (required), competition retention-outreach
- measurement: journey exit-or-handoff (5 refs); business relationship_recovered self · touched-before-event · pre-post
- discovery: 4 aliases
- reviewer: Keyed on the account and the retention episode. Outcome measured on the relationship recovering, compared pre/post. (changed: entity.instanceKey, journey.measurement.businessOutcome, journey.implementation.attributes.required, aliases, useCases)

### RET-31 · Predicted Need Replenishment — NEW canonical journey (Gate 3; communicating)
- entity instance: `person_id + need_key` · one-active-per-key · supersession stated
- eligibility 5 · suppressions 7 (CCCCCCO)
- waits → Config: w.lead: replenishment.lead_time = 3 days–7 days [example-only] [reminder-before-attribute, attribute@expected_depletion_at]; w.window: replenishment.follow_margin = 3 days–7 days [example-only] [observation-window, attribute@expected_depletion_at]; w.final: replenishment.lifetime = 14 days–30 days [example-only] [observation-window, previous-touch]
- exits classed: x.replenished=success, x.dismissed=suppression, x.no-action=no-action, x.lapsed=timeout
- orchestration: deadline-countdown, 2 touch(es) [t1←w.lead:persistent>low-friction, t2←w.window after t1:persistent>low-friction], no-action 6; contact promotional/promotional, cap replenishment.touches = 2 [corpus-rule] (all), cooldown replenishment.cooldown = until the next predicted depletion for the same need [corpus-rule], competition commerce-recovery
- measurement: journey exit (4 refs); business purchase_completed self · touched-before-event · persistent-holdout
- discovery: 6 aliases, presets predicted-next-purchase

### RET-32 · Lapsed Customer Win-Back — NEW canonical journey (Gate 3; communicating)
- entity instance: `person_id + relationship_id` · one-active-per-key · supersession stated
- eligibility 5 · suppressions 7 (CCCCCCO)
- waits → Config: w.response: winback.response_window = 14 days–30 days [example-only] [response-window, previous-touch]; w.final: winback.lifetime = 30 days–60 days [example-only] [observation-window, previous-touch]
- exits classed: x.won=success, x.no-action=no-action, x.lapsed=timeout
- orchestration: offer-decide-remind, 2 touch(es) [t1:persistent>low-friction, t2←w.response after t1:persistent>low-friction], no-action 6; contact promotional/promotional, cap winback.touches = 2 [corpus-rule] (all), cooldown winback.cooldown = 180 days–365 days [example-only], competition retention-outreach
- measurement: journey exit (3 refs); business relationship_repaid self · touched-before-event · persistent-holdout
- discovery: 7 aliases

### RLT-279 · Upgrade Blocker Reminder — migrated (Gate 4; communicating)
- entity instance: `target_id + change_id` · one-active-per-key
- eligibility 5 · suppressions 4 (CCCC)
- waits → Config: w.clear: upgrade_blocker.clear (required) [attribute-bound, attribute@preparation_window_ends_at]; w.final: upgrade_blocker.final (required) [attribute-bound, attribute@preparation_window_ends_at]
- exits classed: x.held=success, x.moot=invalid-state, x.unprepared=timeout
- orchestration: offer-decide-remind, 3 touch(es) [t1:in-session>persistent, t2:in-session>persistent, t3←w.clear after t2:in-session>persistent], no-action 4; contact service/service, cap upgrade_blocker.touches = 2 [corpus-rule] (all), cooldown upgrade_blocker.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business named_requirement_satisfied self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the target and the change. The hold notice and the blocker prompt are the exclusive arms of c.resolvable; the blocker prompt and the last call carry the clearing route. Both waits are bound to the preparation window. Two prompts at most, per the corpus. Outcome: the prerequisite cleared, compared pre/post. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.measurement.businessOutcome, journey.implementation.attributes.required, touch t2, touch t3, wait w.clear, wait w.final, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): The hold notice and the blocker prompt are the exclusive arms of c.resolvable.

### RSK-273 · Usage Limit Alert — migrated (Gate 4; communicating)
- entity instance: `entity_ref + limit_id + window_id` · one-active-per-key
- eligibility 4 · suppressions 4 (CCCC)
- waits → Config: w.capacity: usage_limit.capacity (required) [attribute-bound, attribute@window_resets_at]
- exits classed: x.reset=success, x.blocked=no-action
- orchestration: offer-decide-remind, 5 touch(es) [t1 mandatory:in-session>persistent, t2 after t1:in-session>persistent, t3:in-session>persistent, t4 after t3:in-session>persistent, t5←w.capacity mandatory:in-session>persistent], no-action 4; contact service/service, cap usage_limit.touches = 2 [corpus-rule] (non-mandatory), cooldown usage_limit.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (3 refs); business capacity_authorised self · touched-before-event · not-applicable
- discovery: 5 aliases
- reviewer: Keyed on the entity, the limit and the window. A usage limit is service communication, not security. Only the wall notice and the reset notice are mandatory; the offers are discretionary. Self-offer and holder-offer are the exclusive arms of c.decider, and the blocked-party notice follows the holder-offer; both offers carry the capacity route. The capacity wait is bound to the window's reset. Blocked with no route is a no-action state. (changed: entity.instanceKey, journey.contact.defaultPriority, journey.contact.pressureClass, journey.contact.localCap.value.default.value, journey.contact.localCap.value.rule, journey.implementation.attributes.required, touch t2, touch t3, touch t4, wait w.capacity, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t2, t3, t4): Self-offer and holder-offer are the exclusive arms of c.decider; the blocked-party notice follows the holder-offer in sequence.

### SCH-172 · Temporary Slot Hold — migrated (Gate 4; silent lifecycle state)
- entity instance: `hold_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.hold: slot_hold.hold (required) [attribute-bound, attribute@expires_at]
- exits classed: x.consumed=success, x.expired=timeout, x.released=success
- measurement: journey exit (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the hold. Consumption into a confirmed reservation is the hold's success. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### SCH-173 · Reservation Validation — migrated (Gate 4; silent lifecycle state)
- entity instance: `reservation_request_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.pending: reservation_request.pending (required) [observation-window, trigger]
- exits classed: x.already=success, x.lapsed=timeout
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the reservation request. (changed: entity.instanceKey, journey.implementation.attributes.required, aliases, useCases)

### SCH-174 · Reservation Readiness — migrated (Gate 3; silent lifecycle state)
- entity instance: `booking_id` · one-active-per-key
- eligibility 3 · suppressions 2 (CC)
- waits → Config: w.prepare: scheduling.readiness_checkpoint (required) [reminder-before-attribute, attribute@scheduled_at]
- exits classed: x.superseded=invalid-state, x.upcoming=success
- measurement: journey exit-or-handoff (3 refs); business prerequisites_completed self · entered-before-event · not-applicable
- discovery: 5 aliases

### SCH-175 · Reschedule Validation — migrated (Gate 4; silent lifecycle state)
- entity instance: `booking_id + reschedule_request_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: x.original-stands=failure, x.rescheduled=success
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the booking and the reschedule request. No move taking place is a failure of the request, with the original preserved. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)

### SCH-176 · Reservation Cancellation Reconciliation — migrated (Gate 4; silent lifecycle state)
- entity instance: `booking_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.cancelled=success
- measurement: journey exit-or-handoff (5 refs); no business outcome
- discovery: 4 aliases
- reviewer: A clean cancellation is the success of this reconciliation. (changed: exits, aliases, useCases)

### SCH-177 · Pre-Service Revalidation — migrated (Gate 2; silent lifecycle state)
- entity instance: `booking_id + occurrence_id` · one-active-per-key
- eligibility 2 · suppressions 2 (CC)
- waits → Config: w.arrival: scheduling.arrival_window = scheduled_at plus the arrival tolerance policy records [attribute-bound] [attribute-bound, attribute@scheduled_at]
- exits classed: x.suppressed=invalid-state
- measurement: journey exit-or-handoff (5 refs); business attendance_recorded self · entered-before-event · not-applicable
- discovery: 4 aliases

### SCH-178 · Service Completion — migrated (Gate 4; silent lifecycle state)
- entity instance: `booking_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.service: service_attendance.service (required) [observation-window, trigger]
- exits classed: x.completed=success
- loop budgets: a.interrupt: service_attendance.interrupt_budget (required)
- measurement: journey exit-or-handoff (6 refs); no business outcome
- discovery: 4 aliases
- reviewer: Accepted as drafted. (changed: aliases, useCases)

### SCH-179 · No-Show Validation — migrated (Gate 4; silent lifecycle state)
- entity instance: `booking_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.suppressed=suppression, x.closed=success
- measurement: journey exit-or-handoff (7 refs); no business outcome
- discovery: 4 aliases
- reviewer: Accepted as drafted. (changed: aliases, useCases)

### SCH-180 · Booking Reschedule — migrated (Gate 4; communicating)
- entity instance: `booking_id + provider_failure_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.reallocated=success, x.cancelled-provider=failure
- orchestration: notice-then-confirm, 2 touch(es) [t1:persistent>urgent, t2:persistent>urgent], no-action 4; contact service/service, cap provider_cancellation.touches = 2 [corpus-rule] (all), cooldown provider_cancellation.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (5 refs); no business outcome
- discovery: 5 aliases
- reviewer: Keyed on the booking and the provider failure. The reallocation notice is informational and carries no route by design. A provider-side cancellation is a failure of the commitment, recorded as ours. (changed: entity.instanceKey, journey.implementation.attributes.required, exits, aliases, useCases)
- warnings reviewed: touch_no_destination (t1): The reallocation notice tells the customer what changed while the time stands; it asks for nothing by design.

### SCH-266 · Appointment Reminder — migrated (Gate 2; communicating)
- entity instance: `booking_id + occurrence_id` · one-active-per-key · supersession stated
- eligibility 4 · suppressions 5 (CCCCC)
- waits → Config: w.prereq: scheduling.pre_start_window = 24 hours–72 hours [example-only] [reminder-before-attribute, attribute@scheduled_at]; w.prestart: scheduling.pre_start_window = 24 hours–72 hours [example-only] [reminder-before-attribute, attribute@scheduled_at]
- exits classed: x.superseded=invalid-state
- orchestration: deadline-countdown, 3 touch(es) [t1:persistent>in-session, t2←w.prestart mandatory:urgent>persistent, t3←w.prestart:urgent>persistent>in-session], no-action 5; contact service/service, cap scheduling.reminder_touches = 2 [corpus-rule] (non-mandatory), cooldown scheduling.reminder_cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (2 refs); business attendance_recorded handoff-chain→SCH-177 · entered-before-event · pre-post
- discovery: 5 aliases
- warnings reviewed: simultaneous_review (touches t2, t3): t2 (at-risk notice) and t3 (reminder) are the two exclusive arms of c.critical - a critical prerequisite is either missing or it is not. They are never sent together; no simultaneous rule is needed.

### SCH-277 · Booking Confirmation — migrated (Gate 4; communicating)
- entity instance: `booking_id` · one-active-per-key
- eligibility 4 · suppressions 5 (CCCCC)
- waits → Config: w.outcome: reservation_outcome.outcome (required) [observation-window, previous-touch]; w.choice: reservation_outcome.choice (required) [attribute-bound, attribute@choice_deadline_at]
- exits classed: x.confirmed=success, x.declined=success, x.lapsed=timeout
- orchestration: offer-decide-remind, 5 touch(es) [t1:persistent>urgent, t2←w.outcome after t1:persistent>urgent, t3←w.outcome:persistent>urgent, t4←w.outcome:persistent>urgent, t5←w.outcome after t1:persistent>urgent], no-action 5; contact service/service, cap reservation_outcome.touches = 2 [corpus-rule] (all), cooldown reservation_outcome.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business booking_confirmed self · touched-before-event · not-applicable
- discovery: 5 aliases
- reviewer: Confirmation, re-offer and decline are the exclusive arms of c.outcome; the re-offer carries the slots and the choice wait is bound to the choosing deadline. The lapse notice follows the acknowledgement. At most two touches reach one requester. (changed: journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.implementation.attributes.required, touch t3, touch t5, wait w.choice, aliases, useCases)
- warnings reviewed: simultaneous_review (t2, t3, t4): Confirmation, re-offer and decline are the exclusive arms of c.outcome - what revalidation decided.

### SCH-280 · No-Show Follow-Up — migrated (Gate 3; communicating)
- entity instance: `booking_id + occurrence_id` · one-active-per-key
- eligibility 4 · suppressions 5 (CCCCC)
- waits → Config: w.rebook: no_show.rebooking_window = 7 days–14 days [example-only] [response-window, previous-touch]
- exits classed: x.suppressed=suppression, x.rebooked=success, x.closed=timeout
- orchestration: single-notice, 2 touch(es) [t-offer:persistent>urgent, t-ack:persistent], no-action 5; contact service/service, cap no_show.touches = 1 [corpus-rule] (all), cooldown no_show.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business rebooked self · touched-before-event · pre-post
- discovery: 6 aliases
- warnings reviewed: touch_no_destination (t-ack): The acknowledgement deliberately carries no route: there is nothing to rebook onto, and a rebooking prompt with nothing behind it is the false call to action the journey forbids. | simultaneous_review (t-offer, t-ack): t-offer and t-ack are the two exclusive arms of c.rebookable - rebooking is available or it is not. Never both.

### SCH-282 · Availability Search Abandonment — migrated (Gate 4; communicating)
- entity instance: `person_id + availability_query_id` · one-active-per-key
- eligibility 4 · suppressions 5 (CCCCC)
- waits → Config: w.settle: availability_searched.settle (required) [recovery-window, trigger]; w.waitlist: availability_searched.waitlist (required) [observation-window, previous-touch]; w.respond: availability_searched.respond (required) [observation-window, previous-touch]
- exits classed: x.no-route=no-action, x.booked=success, x.waitlisted=success, x.nothing=no-action, x.lapsed=timeout
- orchestration: offer-decide-remind, 2 touch(es) [t1←w.settle:persistent>low-friction, t2←w.settle:persistent>low-friction], no-action 5; contact promotional/promotional, cap availability_searched.touches = 2 [corpus-rule] (all), cooldown availability_searched.cooldown = none [corpus-rule], competition commerce-recovery
- measurement: journey exit (5 refs); business booking_confirmed self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the person and the availability query. An unprompted offer after a search is commercial recovery: promotional pressure, in the commerce-recovery group. The offer and the waitlist are the exclusive arms of c.options and each carries its route. The settle wait is a recovery window from the query. Exit classes corrected. Outcome compared pre/post. (changed: entity.instanceKey, journey.contact.defaultPriority, journey.contact.pressureClass, journey.contact.competition, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t1, touch t2, wait w.settle, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): The nearest-window offer and the waitlist offer are the exclusive arms of c.options.

### SUB-161 · Relationship Activation — migrated (Gate 4; silent lifecycle state)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.requirement: continuing_relationship.requirement (required) [observation-window, trigger]
- exits classed: x.never-active=invalid-state
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the relationship record. (changed: entity.instanceKey, journey.implementation.attributes.required, aliases, useCases)

### SUB-162 · Future Activation Revalidation — migrated (Gate 4; silent lifecycle state)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.effective: future_effective.effective (required) [attribute-bound, attribute@effective_at]; w.hold: future_effective.hold (required) [observation-window, trigger]
- exits classed: x.cancelled-before-start=invalid-state, x.failed=failure
- loop budgets: a.note: future_effective.note_budget (required)
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. The pre-start wait runs to the effective time. Not activating at the effective date is a failure. (changed: wait w.effective, exits, trigger, aliases, useCases)

### SUB-163 · Renewal Reminder — migrated (Gate 3; communicating)
- entity instance: `relationship_id + renewal_cycle_id` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- waits → Config: w.decision: renewal.decision_deadline = term_end_at minus the notice period the terms require [attribute-bound] [attribute-bound, attribute@term_end_at]; w.review: renewal.notice_deadline = term_end_at minus the notice period the terms require [attribute-bound] [attribute-bound, attribute@term_end_at]
- exits classed: 
- orchestration: deadline-countdown, 2 touch(es) [t-notice mandatory:persistent>in-session, t-request:persistent>in-session>urgent], no-action 5; contact service/service, cap renewal.discretionary_touches = 1 [corpus-rule] (non-mandatory), cooldown renewal.cooldown = none [corpus-rule], competition relationship-continuity
- measurement: journey handoff (4 refs); business renewal_decision_recorded self · entered-before-event · not-applicable
- discovery: 6 aliases
- warnings reviewed: router_journey: Renewal reminder ends only in handoffs by design: a decided renewal executes (SUB-164), a non-renewal schedules its end (SUB-168), an undefined term or an overrun review goes to decision or ownership. The decision is the outcome and each decision has an owner.

### SUB-165 · Renewal Payment Recovery — migrated (Gate 3; silent lifecycle state)
- entity instance: `subscription_id + renewal_cycle_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.recovery: renewal_payment.grace_deadline = the grace deadline the policy defines for this subscription class [attribute-bound] [attribute-bound, attribute@grace_deadline_at]
- exits classed: 
- measurement: journey handoff (3 refs); business obligation_satisfied self · entered-before-event · not-applicable
- discovery: 5 aliases
- warnings reviewed: router_journey: Renewal payment recovery is a relationship-state holder: it ends in completion, end or decision resolution, all owned elsewhere. Its communication is FIN-134's.

### SUB-166 · Plan Change Validation — migrated (Gate 4; silent lifecycle state)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.effective: terms_change.effective (required) [attribute-bound, attribute@change_effective_at]
- exits classed: x.rejected=invalid-state, x.void=invalid-state, x.not-applied=invalid-state, x.applied=success
- measurement: journey exit-or-handoff (8 refs); no business outcome
- discovery: 4 aliases
- reviewer: The scheduled-change wait runs to the change's effective time. A rejected or not-applied change leaves the terms as they were: invalid-state outcomes. (changed: wait w.effective, exits, aliases, useCases)

### SUB-167 · Cancellation Effective-Date Resolution — migrated (Gate 4; silent lifecycle state)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- exits classed: x.blocked=failure
- measurement: journey exit-or-handoff (5 refs); no business outcome
- discovery: 4 aliases
- reviewer: Accepted as drafted. (changed: aliases, useCases)

### SUB-168 · Scheduled Cancellation Revalidation — migrated (Gate 4; silent lifecycle state)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: x.suppressed=suppression
- measurement: journey exit-or-handoff (2 refs); no business outcome
- discovery: 3 aliases
- reviewer: Named the proxies. (changed: trigger, aliases, useCases)

### SUB-169 · Relationship Suspension — migrated (Gate 4; silent lifecycle state)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.suspension: relationship_suspension.suspension (required) [attribute-bound, attribute@maximum_suspension_ends_at]
- exits classed: x.restored=success
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: The suspension wait is bound to the maximum duration policy defines. (changed: wait w.suspension, aliases, useCases)

### SUB-170 · Continuing Relationship End Reconciliation — migrated (Gate 4; silent lifecycle state)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- waits → Config: w.winddown: continuing_relationship.winddown (required) [observation-window, trigger]
- exits classed: x.former=success
- measurement: journey exit-or-handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Reaching the terminal state is the success of this journey; the wind-down wait is an observation window. (changed: wait w.winddown, exits, aliases, useCases)

### SUB-262 · Cancellation Confirmation — migrated (Gate 4; communicating)
- entity instance: `relationship_id` · one-active-per-key
- eligibility 4 · suppressions 4 (CCCC)
- waits → Config: w.window: cancellation_wind.window (required) [attribute-bound, attribute@effective_end_at]
- exits classed: x.returned=invalid-state, x.ended=success
- orchestration: offer-decide-remind, 2 touch(es) [t1:persistent>in-session, t2←w.window after t1:persistent>in-session], no-action 4; contact service/service, cap cancellation_wind.touches = 2 [corpus-rule] (all), cooldown cancellation_wind.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (3 refs); no business outcome
- discovery: 4 aliases
- reviewer: The confirmation carries no route by design. The window wait is bound to the effective end date. Ending cleanly is the success; a withdrawn cancellation is an invalid state for this journey, which never re-litigates. No business outcome: the journey exists to make the end date unsurprising, not to win anyone back. (changed: journey.implementation.attributes.required, -journey.measurement.businessOutcome, wait w.window, exits, aliases, useCases)
- warnings reviewed: touch_no_destination (t1): The cancellation confirmation states the end date and what remains; it asks for nothing by design and never re-litigates.

### TIM-268 · Action Required Reminder — migrated (Gate 4; communicating)
- entity instance: `obligation_id` · one-active-per-key
- eligibility 4 · suppressions 5 (CCCCC)
- waits → Config: w.due: outstanding_obligation.due (required) [reminder-before-attribute, attribute@due_at]; w.deadline: outstanding_obligation.deadline (required) [attribute-bound, attribute@due_at]
- exits classed: x.moot=no-action, x.satisfied=success, x.lapsed=timeout
- orchestration: offer-decide-remind, 3 touch(es) [t1←w.due:persistent>urgent, t2←w.deadline after t1:persistent>urgent, t3←w.deadline after t1:persistent>urgent], no-action 5; contact service/service, cap outstanding_obligation.touches = 3 [corpus-rule] (all), cooldown outstanding_obligation.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business obligation_satisfied self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: The reminder is gated by the pre-deadline wait; the confirmation and the overdue notice follow it as the two arms of the deadline wait. The reminder carries the discharge route; the confirmation carries none by design. The deadline wait is bound to the due date. A moot obligation is a no-action outcome. Outcome compared pre/post. (changed: journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t1, touch t3, wait w.deadline, exits, aliases, useCases)
- warnings reviewed: touch_no_destination (t2): The discharge confirmation asks for nothing; it closes the loop and carries no route by design. | simultaneous_review (t2, t3): The discharge confirmation and the overdue notice are the exclusive arms of c.settled at the deadline.

### TIM-274 · Grace Period Recovery — migrated (Gate 4; communicating)
- entity instance: `entity_ref + grace_period_id` · one-active-per-key
- eligibility 5 · suppressions 5 (CCCCC)
- waits → Config: w.grace: grace_period.grace (required) [reminder-before-attribute, attribute@grace_deadline_at]; w.final: grace_period.final (required) [attribute-bound, attribute@grace_deadline_at]
- exits classed: x.recovered=success, x.moot=invalid-state, x.lost=timeout
- orchestration: offer-decide-remind, 5 touch(es) [t1:persistent>urgent, t2:persistent>urgent, t3←w.grace:persistent>urgent, t4←w.grace:persistent>urgent, t5←w.final after t4:persistent>urgent], no-action 5; contact service/service, cap grace_period.touches = 3 [corpus-rule] (all), cooldown grace_period.cooldown = none [corpus-rule], competition none
- measurement: journey exit (3 refs); business recovery_condition_satisfied self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the entity and its grace period. The restricted and quiet notices are the exclusive arms of c.restricted. The final wait routes through c.outcome so the confirmation is a decision, not a send on the success event. The loss notice carries the route back on new terms; the confirmation carries none. Both waits are bound to the grace end. Early termination is an invalid state. The longest path sends three touches. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t5, wait w.final, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2): The restricted notice and the quiet notice are the exclusive arms of c.restricted - whether grace reduces anything the holder will notice. | touch_no_destination (t3): The recovery confirmation asks for nothing; it names what returned and carries no route by design.

### TIM-281 · Expired Access Recovery — migrated (Gate 4; communicating)
- entity instance: `entity_ref + attempt_id` · one-active-per-key
- eligibility 5 · suppressions 4 (CCCC)
- waits → Config: w.act: expired_entity.act (required) [response-window, previous-touch@expires_at]
- exits classed: x.restored=success, x.still-expired=timeout, x.terminal=invalid-state
- orchestration: offer-decide-remind, 5 touch(es) [t1:persistent>in-session, t2:persistent>in-session, t3:persistent>in-session, t4:persistent>in-session, t5←w.act:persistent>in-session], no-action 4; contact service/service, cap expired_entity.touches = 2 [corpus-rule] (all), cooldown expired_entity.cooldown = none [corpus-rule], competition none
- measurement: journey exit (3 refs); business validity_restored self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the expired entity and the holder's attempt. The four route statements are the exclusive arms of c.route and each carries its route; the dead end and the confirmation carry none by design. The wait after naming the route is a response window from the touch, not the expiry. At most two touches reach one person. No route back is an invalid state. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.default.applicableWhen, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t1, touch t2, touch t3, wait w.act, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t1, t2, t3, t4): The four route statements are the exclusive arms of c.route - which mechanism restores validity. Exactly one is ever sent. | touch_no_destination (t5): The confirmation of restored validity asks for nothing and carries no route by design.

### TIM-61 · Deadline Tracking — migrated (Gate 4; communicating)
- entity instance: `obligation_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.tracking: deadline_tracking.tracking (required) [attribute-bound, attribute@due_at]
- exits classed: x.satisfied=success, x.failed=failure, x.still-valid=no-action
- loop budgets: a.remind: deadline_tracking.remind_budget (required)
- orchestration: single-notice, 1 touch(es) [t1←w.tracking:persistent>low-friction>urgent], no-action 4; contact service/service, cap deadline_tracking.touches (required) (all), cooldown deadline_tracking.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (6 refs); business obligation_satisfied self · touched-before-event · pre-post
- discovery: 5 aliases
- reviewer: Keyed on the obligation. The reminder carries the discharge route; the number of reminders is the number of thresholds policy defines, a required config. A deadline that passes with the obligation unchanged is a no-action outcome. Outcome compared pre/post. (changed: entity.instanceKey, journey.contact.localCap, journey.measurement.businessOutcome.comparison, journey.implementation.attributes.required, touch t1, exits, aliases, useCases)

### TIM-62 · Overdue State Recalculation — migrated (Gate 4; silent lifecycle state)
- entity instance: `obligation_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- exits classed: x.stale=suppression, x.tracked=success
- measurement: journey exit-or-handoff (5 refs); no business outcome
- discovery: 4 aliases
- reviewer: Accepted as drafted. (changed: aliases, useCases)

### TIM-63 · Expiry Reminder — migrated (Gate 3; communicating)
- entity instance: `entity_ref + validity_id` · one-active-per-key
- eligibility 3 · suppressions 5 (CCCCC)
- waits → Config: w.resolution: expiry.expiry_moment = expires_at as the system of record asserts it [attribute-bound] [attribute-bound, attribute@expires_at]
- exits classed: x.suppressed=suppression, x.silent=no-action
- orchestration: deadline-countdown, 2 touch(es) [t-prompt:persistent>in-session>urgent, t-inform:persistent], no-action 5; contact service/service, cap expiry.touches = 1 [corpus-rule] (all), cooldown expiry.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (4 refs); business renewed self · touched-before-event · pre-post
- discovery: 7 aliases

### TIM-65 · Grace Period Management — migrated (Gate 2; silent lifecycle state)
- entity instance: `entity_id + grace_period_id` · one-active-per-key
- eligibility 2 · suppressions 2 (CC)
- waits → Config: w.grace: time.grace_end = grace_end as recorded at entry [attribute-bound] [attribute-bound, attribute@grace_end]
- exits classed: x.recovered=success
- measurement: journey exit-or-handoff (4 refs); business recovery_condition_satisfied self · entered-before-event · not-applicable
- discovery: 4 aliases

### TRM-105 · Responsibility Handover — migrated (Gate 4; silent lifecycle state)
- entity instance: `role_id + handover_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.effective: responsibility_handover.effective (required) [attribute-bound, attribute@effective_at]
- exits classed: x.cancelled=invalid-state, x.handed-over=success
- measurement: journey exit-or-handoff (4 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the role and the handover. The wait runs to the handover's effective time. (changed: entity.instanceKey, journey.implementation.attributes.required, wait w.effective, trigger, aliases, useCases)

### TRM-106 · Account Closure — migrated (Gate 4; communicating)
- entity instance: `account_id + closure_request_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.blockers: account_closure.blockers (required) [observation-window, previous-touch]
- exits classed: x.unauthorized=invalid-state, x.still-blocked=failure, x.lapsed=timeout, x.cannot-close=failure
- orchestration: notice-then-confirm, 4 touch(es) [t1 mandatory:persistent>in-session, t2 mandatory:persistent>in-session, t3 mandatory:persistent>in-session, t4 mandatory:persistent>in-session], no-action 4; contact transactional/none, cap account_closure.touches = 0 [corpus-rule] (non-mandatory), cooldown account_closure.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (6 refs); no business outcome
- discovery: 4 aliases
- reviewer: Keyed on the account and the closure request. Every touch is mandatory (transactional), so the discretionary cap is zero. The recoverable and non-recoverable blocker notices are the exclusive arms of c.blockers; the blocker notice carries the blocker, the closure confirmation carries no route by design. Exit classes corrected: an unauthorised request is an invalid state, a blocked closure is a failure of the request. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.rule, journey.implementation.attributes.required, touch t4, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t2, t4): The non-recoverable notice (t2) and the recoverable blocker (t4) are the exclusive arms of c.blockers. | touch_no_destination (t3): The closure confirmation states what closed and what closure is not; it asks for nothing by design.

### TRM-107 · Account Closure Reconciliation — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id + closure_id` · one-active-per-key
- eligibility 3 · suppressions 4 (CCCC)
- waits → Config: w.outcomes: closure_external.outcomes (required) [observation-window, trigger]
- exits classed: x.finalized=success
- measurement: journey exit-or-handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the account and the closure. (changed: entity.instanceKey, journey.implementation.attributes.required, trigger, aliases, useCases)

### TRM-108 · Account Closure Wind-Down — migrated (Gate 4; silent lifecycle state)
- entity instance: `account_id` · one-active-per-key
- eligibility 3 · suppressions 3 (CCC)
- waits → Config: w.winddown: closure_wind.winddown (required) [observation-window, trigger]
- exits classed: x.former=success
- measurement: journey exit-or-handoff (2 refs); no business outcome
- discovery: 4 aliases
- reviewer: Named the proxies. Keyed on the account. (changed: entity.instanceKey, journey.implementation.attributes.required, trigger, aliases, useCases)

### TRM-275 · Data Deletion Confirmation — migrated (Gate 4; communicating)
- entity instance: `deletion_request_id` · one-active-per-key
- eligibility 5 · suppressions 5 (CCCCC)
- waits → Config: w.verify: deletion_request.verify (required) [response-window, previous-touch]; w.decision: deletion_request.decision (required) [attribute-bound, attribute@response_deadline_at]
- exits classed: x.unverified=invalid-state, x.closed=success
- orchestration: offer-decide-remind, 5 touch(es) [t1 mandatory:persistent, t2 after t1 mandatory:persistent, t3←w.verify after t2 mandatory:persistent, t4←w.decision mandatory:persistent, t5←w.decision mandatory:persistent], no-action 5; contact transactional/none, cap deletion_request.touches = 0 [corpus-rule] (non-mandatory), cooldown deletion_request.cooldown = none [corpus-rule], competition none
- measurement: journey exit-or-handoff (3 refs); business deletion_resolved self · entered-before-event · not-applicable
- discovery: 5 aliases
- reviewer: Keyed on the deletion request. Every touch is mandatory (transactional), so the discretionary cap is zero. The verification request carries the proof route; the two closure records are the exclusive arms of c.outcome and carry no route by design. The decision wait is bound to the obligation's response deadline. An unverified close is an invalid state. Outcome: the deletion resolved. (changed: entity.instanceKey, journey.contact.localCap.value.default.value, journey.contact.localCap.value.rule, journey.measurement.businessOutcome, journey.implementation.attributes.required, touch t2, wait w.decision, exits, aliases, useCases)
- warnings reviewed: simultaneous_review (t4, t5): Full and partial closure records are the exclusive arms of c.outcome. | touch_no_destination (t4): The closure record is the durable answer to the request; it asks for nothing by design.

