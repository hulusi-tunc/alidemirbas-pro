# Lowering notes

Generated by `scripts/lowering-fixture.mjs`. Each fixture in this directory lists the platform-neutral primitives the journey reduces to. The notes below say, per construct the journey uses, how typical orchestration platforms carry it. **No deployment was performed; nothing here claims a tested integration.** "EXPENSIVE" and "PARTIAL" are honest markers, not blockers.

## ACQ-04 · High-Intent Lead Routing

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `handoff`, `human-task` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|

## ACQ-09 · Lead Nurture

Strategy: `single-notice` · primitives used: `entry`, `condition`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `state-update`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## ACQ-285 · New Lead Welcome

Strategy: `notice-then-confirm` · primitives used: `entry`, `condition`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `handoff`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## ACQ-11 · Abandoned Process Recovery

Strategy: `progressive-recovery` · primitives used: `entry`, `holdout`, `condition`, `state-update`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `frequency-gate`, `channel-role-resolution`, `message`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| persistent holdout | Canvas control group / global control group - supported. | Control group - supported. | Random split to a persistent holdout data extension - supported. | Holdout groups / Customer.io experiments - supported. |
| bounded re-arm / attempt budget | A counter custom attribute incremented per re-arm and tested at the split - supported with a small custom attribute. | Counter attribute - supported. | Counter in a data extension updated by an Update Contact activity - supported. | Counter profile field - supported. |

## ACQ-12 · Abandoned Selection Recovery

Strategy: `progressive-recovery` · primitives used: `entry`, `holdout`, `condition`, `state-update`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `frequency-gate`, `channel-role-resolution`, `message`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| persistent holdout | Canvas control group / global control group - supported. | Control group - supported. | Random split to a persistent holdout data extension - supported. | Holdout groups / Customer.io experiments - supported. |
| bounded re-arm / attempt budget | A counter custom attribute incremented per re-arm and tested at the split - supported with a small custom attribute. | Counter attribute - supported. | Counter in a data extension updated by an Update Contact activity - supported. | Counter profile field - supported. |

## ACQ-13 · Unresolved Interest Recovery

Strategy: `single-notice` · primitives used: `entry`, `holdout`, `condition`, `state-update`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `frequency-gate`, `channel-role-resolution`, `message`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| persistent holdout | Canvas control group / global control group - supported. | Control group - supported. | Random split to a persistent holdout data extension - supported. | Holdout groups / Customer.io experiments - supported. |

## ACT-11 · Onboarding Route Assignment

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `human-task`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## ACT-12 · Onboarding Nurture

Strategy: `progressive-recovery` · primitives used: `entry`, `holdout`, `state-update`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `handoff`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| persistent holdout | Canvas control group / global control group - supported. | Control group - supported. | Random split to a persistent holdout data extension - supported. | Holdout groups / Customer.io experiments - supported. |
| bounded re-arm / attempt budget | A counter custom attribute incremented per re-arm and tested at the split - supported with a small custom attribute. | Counter attribute - supported. | Counter in a data extension updated by an Update Contact activity - supported. | Counter profile field - supported. |

## ACT-13 · Onboarding Blocker Reminder

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `human-task`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## ACT-14 · Onboarding Help

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## ACT-17 · Adoption Nurture

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `condition`, `handoff`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| bounded re-arm / attempt budget | A counter custom attribute incremented per re-arm and tested at the split - supported with a small custom attribute. | Counter attribute - supported. | Counter in a data extension updated by an Update Contact activity - supported. | Counter profile field - supported. |

## ACT-18 · Adoption Recovery

Strategy: `single-notice` · primitives used: `entry`, `holdout`, `state-update`, `condition`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| persistent holdout | Canvas control group / global control group - supported. | Control group - supported. | Random split to a persistent holdout data extension - supported. | Holdout groups / Customer.io experiments - supported. |

## ACT-19 · Onboarding Personalization

Strategy: `single-notice` · primitives used: `entry`, `condition`, `state-update`, `idempotency-key`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## ACT-20 · Dormant Lead Reactivation

Strategy: `single-notice` · primitives used: `entry`, `condition`, `exit`, `state-update`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## RET-24 · Churn Risk Escalation

Strategy: `single-notice` · primitives used: `entry`, `condition`, `handoff`, `state-update`, `idempotency-key`, `human-task`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|

## RET-26 · Service Recovery

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `condition`, `exit`, `handoff`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## RET-28 · Cancellation Save

Strategy: `offer-decide-remind` · primitives used: `entry`, `state-update`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `handoff`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## RET-30 · Retention Offer Follow-Up

Strategy: `single-notice` · primitives used: `entry`, `wait`, `event-cancellation`, `state-recheck`, `condition`, `state-update`, `idempotency-key`, `handoff`, `exit`, `frequency-gate`, `channel-role-resolution`, `message` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## RET-31 · Predicted Need Replenishment

Strategy: `deadline-countdown` · primitives used: `entry`, `holdout`, `condition`, `state-update`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `frequency-gate`, `channel-role-resolution`, `message`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| persistent holdout | Canvas control group / global control group - supported. | Control group - supported. | Random split to a persistent holdout data extension - supported. | Holdout groups / Customer.io experiments - supported. |

## RET-32 · Lapsed Customer Win-Back

Strategy: `offer-decide-remind` · primitives used: `entry`, `holdout`, `condition`, `state-update`, `idempotency-key`, `frequency-gate`, `channel-role-resolution`, `message`, `wait`, `event-cancellation`, `state-recheck`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| persistent holdout | Canvas control group / global control group - supported. | Control group - supported. | Random split to a persistent holdout data extension - supported. | Holdout groups / Customer.io experiments - supported. |

## CON-264 · Contact Verification

Strategy: `two-party-confirmation` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `handoff`, `state-update`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## CON-272 · Contact Recovery

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `exit`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## CON-283 · Frequency Preference Update

Strategy: `single-notice` · primitives used: `entry`, `condition`, `exit`, `state-update`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## FBK-41 · Feedback Request

Strategy: `single-notice` · primitives used: `entry`, `condition`, `wait`, `event-cancellation`, `state-recheck`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## FBK-42 · Advocacy Request

Strategy: `single-notice` · primitives used: `entry`, `condition`, `exit`, `state-update`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## FBK-43 · Feedback Follow-Up

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `handoff`, `exit`, `human-task`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## FBK-46 · Complaint Resolution

Strategy: `human-escalation-ladder` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `handoff`, `human-task`, `wait`, `event-cancellation`, `state-recheck`, `frequency-gate`, `channel-role-resolution`, `message`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| bounded re-arm / attempt budget | A counter custom attribute incremented per re-arm and tested at the split - supported with a small custom attribute. | Counter attribute - supported. | Counter in a data extension updated by an Update Contact activity - supported. | Counter profile field - supported. |

## FBK-47 · Appeal Review

Strategy: `offer-decide-remind` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `exit`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| bounded re-arm / attempt budget | A counter custom attribute incremented per re-arm and tested at the split - supported with a small custom attribute. | Counter attribute - supported. | Counter in a data extension updated by an Update Contact activity - supported. | Counter profile field - supported. |

## FBK-49 · Missing Information Reminder

Strategy: `notice-then-confirm` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `human-task`, `wait`, `event-cancellation`, `state-recheck`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## TIM-61 · Deadline Tracking

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `condition`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| bounded re-arm / attempt budget | A counter custom attribute incremented per re-arm and tested at the split - supported with a small custom attribute. | Counter attribute - supported. | Counter in a data extension updated by an Update Contact activity - supported. | Counter profile field - supported. |

## TIM-63 · Expiry Reminder

Strategy: `deadline-countdown` · primitives used: `entry`, `state-update`, `condition`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## TIM-268 · Action Required Reminder

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `wait`, `event-cancellation`, `state-recheck`, `state-update`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## TIM-274 · Grace Period Recovery

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## TIM-281 · Expired Access Recovery

Strategy: `offer-decide-remind` · primitives used: `entry`, `state-update`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## ACC-261 · Access Recovery

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `exit`, `handoff`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## ACC-263 · Activation Reminder

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## IDN-81 · Identity Verification

Strategy: `notice-then-confirm` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `wait`, `event-cancellation`, `state-recheck`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |
| bounded re-arm / attempt budget | A counter custom attribute incremented per re-arm and tested at the split - supported with a small custom attribute. | Counter attribute - supported. | Counter in a data extension updated by an Update Contact activity - supported. | Counter profile field - supported. |

## IDN-84 · Verification Recovery

Strategy: `notice-then-confirm` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## IDN-85 · Login Verification

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `condition`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## IDN-270 · Account Recovery

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `handoff`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## IDN-271 · Account Security Alert

Strategy: `notice-then-confirm` · primitives used: `entry`, `condition`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## REL-284 · Invitation Reminder

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## TRM-106 · Account Closure

Strategy: `notice-then-confirm` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `exit`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## TRM-275 · Data Deletion Confirmation

Strategy: `offer-decide-remind` · primitives used: `entry`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `condition`, `wait`, `event-cancellation`, `state-recheck`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## INT-269 · Integration Recovery

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `exit`, `wait`, `event-cancellation`, `state-recheck`, `state-update`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## INT-278 · Integration Setup

Strategy: `notice-then-confirm` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `exit`, `handoff`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| bounded re-arm / attempt budget | A counter custom attribute incremented per re-arm and tested at the split - supported with a small custom attribute. | Counter attribute - supported. | Counter in a data extension updated by an Update Contact activity - supported. | Counter profile field - supported. |

## FIN-134 · Payment Failure Recovery

Strategy: `notice-then-confirm` · primitives used: `entry`, `state-update`, `condition`, `idempotency-key`, `frequency-gate`, `channel-role-resolution`, `message`, `wait`, `event-cancellation`, `state-recheck`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| downstream outcome through handoff chain | Not an orchestration concept: measured in analytics by joining the entry with the downstream event; the fixture names the chain and the window event. | Same - analytics join, not journey logic. | Same - journey goal can only reference events inside the journey; downstream outcome is a data view join. | Same - conversion tracking is per journey; chain measurement is a warehouse join. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## FIN-137 · Refund Request

Strategy: `notice-then-confirm` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `handoff`, `exit`, `human-task`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## FUL-146 · Delivery Delay Alert

Strategy: `offer-decide-remind` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## FUL-148 · Failed Delivery Recovery

Strategy: `notice-then-confirm` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## FUL-265 · Delivery Tracking

Strategy: `offer-decide-remind` · primitives used: `entry`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `condition`, `exit`, `handoff`, `state-update` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## FUL-276 · Substitution Approval

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `handoff`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## REM-151 · Post-Purchase Issue Recovery

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `exit`, `frequency-gate`, `channel-role-resolution`, `message`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## REM-152 · Return Request

Strategy: `notice-then-confirm` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `handoff`, `frequency-gate`, `channel-role-resolution`, `message`, `exit`, `human-task`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## REM-157 · Remedy Confirmation

Strategy: `notice-then-confirm` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `wait`, `event-cancellation`, `state-recheck`, `handoff`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## SUB-163 · Renewal Reminder

Strategy: `deadline-countdown` · primitives used: `entry`, `state-update`, `condition`, `handoff`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## SUB-262 · Cancellation Confirmation

Strategy: `offer-decide-remind` · primitives used: `entry`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `condition`, `wait`, `event-cancellation`, `state-recheck`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## SCH-180 · Booking Reschedule

Strategy: `notice-then-confirm` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## SCH-266 · Appointment Reminder

Strategy: `deadline-countdown` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `state-update`, `handoff`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| downstream outcome through handoff chain | Not an orchestration concept: measured in analytics by joining the entry with the downstream event; the fixture names the chain and the window event. | Same - analytics join, not journey logic. | Same - journey goal can only reference events inside the journey; downstream outcome is a data view join. | Same - conversion tracking is per journey; chain measurement is a warehouse join. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## SCH-277 · Booking Confirmation

Strategy: `offer-decide-remind` · primitives used: `entry`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `condition`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## SCH-280 · No-Show Follow-Up

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `condition`, `exit`, `handoff`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## SCH-282 · Availability Search Abandonment

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `exit`, `wait`, `event-cancellation`, `state-recheck`, `state-update`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## DEC-184 · More Information Request

Strategy: `notice-then-confirm` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `human-task`, `wait`, `event-cancellation`, `state-recheck`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| bounded re-arm / attempt budget | A counter custom attribute incremented per re-arm and tested at the split - supported with a small custom attribute. | Counter attribute - supported. | Counter in a data extension updated by an Update Contact activity - supported. | Counter profile field - supported. |

## DEC-267 · Adverse Decision Recovery

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `wait`, `event-cancellation`, `state-recheck`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## RSK-273 · Usage Limit Alert

Strategy: `offer-decide-remind` · primitives used: `entry`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `condition`, `wait`, `event-cancellation`, `state-recheck`, `handoff`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## DOC-214 · Document Delivery

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `idempotency-key`, `frequency-gate`, `channel-role-resolution`, `message`, `wait`, `event-cancellation`, `state-recheck`, `condition`, `exit`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## DOC-215 · Signature Reminder

Strategy: `offer-decide-remind` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `wait`, `event-cancellation`, `state-recheck`, `handoff`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |
| bounded re-arm / attempt budget | A counter custom attribute incremented per re-arm and tested at the split - supported with a small custom attribute. | Counter attribute - supported. | Counter in a data extension updated by an Update Contact activity - supported. | Counter profile field - supported. |

## DOC-220 · Document Conflict Review

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `handoff`, `frequency-gate`, `channel-role-resolution`, `message`, `exit` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## DOC-286 · Document Activation

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `exit`, `wait`, `event-cancellation`, `state-recheck` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| multi-event cancellation | Canvas action paths / exit criteria on several events - supported. | Wait-until-event with multiple exit events - supported. | EXPENSIVE: exit criteria per event plus re-entry; a wait activity cancels on one event at a time, so each cancelling event is an exit criterion and touch state is re-derived on re-entry. | Journey exit rules / Customer.io goal & exit conditions on multiple events - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| mandatory touch exempt from caps | Transactional messages bypass frequency capping - supported. | Transactional channel exemption - supported. | Transactional sends via Transactional Messaging API bypass caps - supported. | Transactional message type ignores frequency limits - supported. |

## RLT-279 · Upgrade Blocker Reminder

Strategy: `offer-decide-remind` · primitives used: `entry`, `condition`, `frequency-gate`, `channel-role-resolution`, `message`, `idempotency-key`, `exit`, `wait`, `event-cancellation`, `state-recheck`, `handoff` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| attribute-relative wait | Delay until a custom-attribute date (Canvas delay step: 'until' a date attribute) - supported. | Wait until date attribute - supported. | Wait By Attribute (date) - supported. | Delay until date field / Customer.io wait until attribute - supported. |
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

## INC-254 · Incident Update

Strategy: `single-notice` · primitives used: `entry`, `state-update`, `idempotency-key`, `condition`, `exit`, `frequency-gate`, `channel-role-resolution`, `message` · unsupported constructs: none

| Construct | Braze | Insider | SFMC | Iterable / Customer.io |
|---|---|---|---|---|
| ordered channel-role resolution | One decision split per role condition, each leading to a channel message step - supported (2-3 splits). | Condition steps per role - supported. | Decision splits on session/token/permission attributes before each channel activity - supported. | Branch on profile fields per role - supported. |
| class-level pressure cap | Frequency capping by campaign tag approximates a pressure class - supported with setup. | Global frequency caps by category - supported. | PARTIAL: contact-level send limits are coarse; class caps need Einstein Frequency or custom suppression data extensions. | PARTIAL: frequency management by message type; Customer.io caps are per-campaign - class caps need a custom counter attribute. |

