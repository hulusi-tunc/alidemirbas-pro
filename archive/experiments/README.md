# Archived frontend experiments

The public routes `/experiment-a` and `/experiment-b` were retired from the site.

Their last page implementations are preserved here for reference:

- `experiment-a/page.tsx`
- `experiment-b/page.tsx`

The route files under `src/app/(en)/` now call `notFound()`, so these experiments cannot surface as alternate public versions of the current site design.
